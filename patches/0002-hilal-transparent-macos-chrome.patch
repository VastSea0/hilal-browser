Hilal Browser: enable transparent macOS chrome surfaces

Makes Hilal top-level macOS windows stay non-opaque, enables native
macOS vibrancy behind the browser chrome, and removes the opaque Firefox
backstops that otherwise keep the toolbar, sidebar, and content frame white.
The chrome styling is applied unconditionally on macOS for Hilal builds so
the vertical tabs sidebar, toolbar, and tab panels can sit on translucent
Tahoe-style surfaces instead of opaque Firefox surfaces.

---
diff --git a/browser/extensions/newtab/content-src/styles/activity-stream.scss b/browser/extensions/newtab/content-src/styles/activity-stream.scss
index 4ac1348a809a..f4dc955ff01d 100644
--- a/browser/extensions/newtab/content-src/styles/activity-stream.scss
+++ b/browser/extensions/newtab/content-src/styles/activity-stream.scss
@@ -42,6 +42,23 @@ body {
   overflow-y: scroll;
 }
 
+@media (-moz-platform: macos) {
+  :root {
+    --newtab-background-color: transparent;
+    --newtab-background-color-secondary: light-dark(rgba(255, 255, 255, 0.68), rgba(42, 41, 49, 0.6));
+    --newtab-background-card: light-dark(rgba(255, 255, 255, 0.52), rgba(66, 65, 77, 0.5));
+    --newtab-weather-background-color: light-dark(rgba(255, 255, 255, 0.5), rgba(66, 65, 77, 0.48));
+    --newtab-overlay-color: light-dark(rgba(255, 255, 255, 0.44), rgba(43, 42, 51, 0.42));
+  }
+
+  html,
+  body,
+  #root,
+  .activity-stream {
+    background-color: transparent;
+  }
+}
+
 h1,
 h2 {
   font-weight: var(--font-weight);
diff --git a/browser/extensions/newtab/css/activity-stream.css b/browser/extensions/newtab/css/activity-stream.css
index ab4d30c3c1b4..77d60f6188e2 100644
--- a/browser/extensions/newtab/css/activity-stream.css
+++ b/browser/extensions/newtab/css/activity-stream.css
@@ -338,6 +338,21 @@ body {
   overflow-y: scroll;
 }
 
+@media (-moz-platform: macos) {
+  :root {
+    --newtab-background-color: transparent;
+    --newtab-background-color-secondary: light-dark(rgba(255, 255, 255, 0.68), rgba(42, 41, 49, 0.6));
+    --newtab-background-card: light-dark(rgba(255, 255, 255, 0.52), rgba(66, 65, 77, 0.5));
+    --newtab-weather-background-color: light-dark(rgba(255, 255, 255, 0.5), rgba(66, 65, 77, 0.48));
+    --newtab-overlay-color: light-dark(rgba(255, 255, 255, 0.44), rgba(43, 42, 51, 0.42));
+  }
+  html,
+  body,
+  #root,
+  .activity-stream {
+    background-color: transparent;
+  }
+}
 h1,
 h2 {
   font-weight: var(--font-weight);
diff --git a/browser/themes/shared/browser-colors.css b/browser/themes/shared/browser-colors.css
index 3022f907be0e..0b771f557c65 100644
--- a/browser/themes/shared/browser-colors.css
+++ b/browser/themes/shared/browser-colors.css
@@ -37,6 +37,23 @@
   --content-area-shadow: var(--card-box-shadow);
   --chrome-content-separator-color: var(--card-border-color);
 
+  @media (-moz-platform: macos) {
+    --hilal-glass-toolbar-background: light-dark(rgba(255, 255, 255, 0.18), rgba(28, 28, 34, 0.2));
+    --hilal-glass-toolbar-background-inactive: light-dark(rgba(255, 255, 255, 0.12), rgba(28, 28, 34, 0.14));
+    --hilal-glass-sidebar-background: light-dark(rgba(255, 255, 255, 0.1), rgba(24, 24, 30, 0.12));
+    --hilal-glass-content-background: transparent;
+    --hilal-glass-border-color: light-dark(rgba(30, 30, 36, 0.12), rgba(255, 255, 255, 0.14));
+    --hilal-glass-shadow: 0 18px 42px light-dark(rgba(30, 30, 36, 0.08), rgba(0, 0, 0, 0.24));
+
+    --toolbox-background-color: var(--hilal-glass-toolbar-background);
+    --toolbox-background-color-inactive: var(--hilal-glass-toolbar-background-inactive);
+    --toolbar-background-color: transparent;
+    --sidebar-background-color: transparent;
+    --sidebar-border-color: var(--hilal-glass-border-color);
+    --chrome-content-separator-color: var(--hilal-glass-border-color);
+    --content-area-shadow: var(--hilal-glass-shadow);
+  }
+
   /* stylelint-disable-next-line media-query-no-invalid */
   @media not -moz-pref("sidebar.revamp"), -moz-pref("browser.nova.enabled") {
     &:not([lwtheme]) {
diff --git a/browser/themes/shared/browser-shared.css b/browser/themes/shared/browser-shared.css
index f38e980ba766..68fb1360fbdb 100644
--- a/browser/themes/shared/browser-shared.css
+++ b/browser/themes/shared/browser-shared.css
@@ -248,6 +248,37 @@ body {
   }
 }
 
+@media (-moz-platform: macos) {
+  :root[windowtype="navigator:browser"]:not([macOSNativeFullscreen]) {
+    background: transparent !important;
+  }
+
+  :root:not([macOSNativeFullscreen]) body {
+    background: transparent !important;
+  }
+
+  :root[windowtype="navigator:browser"]:not([macOSNativeFullscreen]) body::after {
+    -moz-default-appearance: -moz-sidebar;
+    appearance: auto;
+  }
+
+  :root:not([macOSNativeFullscreen]) #navigator-toolbox,
+  :root:not([macOSNativeFullscreen]) #sidebar-main,
+  :root:not([macOSNativeFullscreen]) .chrome-block {
+    background-color: var(--hilal-glass-toolbar-background) !important;
+    backdrop-filter: blur(30px) saturate(1.35);
+  }
+
+  :root:not([macOSNativeFullscreen]) .browser-toolbar:not(.browser-titlebar) {
+    background-color: transparent !important;
+  }
+
+  :root:not([macOSNativeFullscreen]) .browserContainer > findbar {
+    background-color: var(--hilal-glass-toolbar-background);
+    backdrop-filter: blur(24px) saturate(1.25);
+  }
+}
+
 /* stylelint-disable-next-line media-query-no-invalid */
 @media -moz-pref("browser.nova.enabled") {
   .chrome-block {
diff --git a/browser/themes/shared/sidebar.css b/browser/themes/shared/sidebar.css
index 90e6bf90ff2f..918ca15d090f 100644
--- a/browser/themes/shared/sidebar.css
+++ b/browser/themes/shared/sidebar.css
@@ -236,6 +236,27 @@
   min-width: 16em;
 }
 
+@media (-moz-platform: macos) {
+  :root:not([macOSNativeFullscreen]) #sidebar-main,
+  :root:not([macOSNativeFullscreen]) #sidebar-box {
+    background: transparent !important;
+    background-image: none !important;
+  }
+
+  :root:not([macOSNativeFullscreen]) #sidebar {
+    background-color: var(--hilal-glass-sidebar-background) !important;
+    backdrop-filter: blur(30px) saturate(1.35);
+    box-shadow: none;
+  }
+
+  :root:not([macOSNativeFullscreen]) #sidebar-main[sidebar-launcher-expanded],
+  :root:not([macOSNativeFullscreen]) #sidebar-main[sidebar-ongoing-animations]:not([sidebar-launcher-expanded]) {
+    background-color: var(--hilal-glass-toolbar-background) !important;
+    background-image: none !important;
+    backdrop-filter: blur(30px) saturate(1.35);
+  }
+}
+
 menuseparator + #sidebar-extensions-separator {
   display: none;
 }
diff --git a/browser/themes/shared/tabbrowser/content-area.css b/browser/themes/shared/tabbrowser/content-area.css
index 522c1f35f50e..422a6c0aaa1b 100644
--- a/browser/themes/shared/tabbrowser/content-area.css
+++ b/browser/themes/shared/tabbrowser/content-area.css
@@ -23,6 +23,12 @@
       --tabpanel-background-color: linear-gradient(45deg, #722291 0%, #45278d 50%, #393473 100%) !important;
     }
   }
+
+  @media (-moz-platform: macos) {
+    &:not([privatebrowsingmode="temporary"]) {
+      --tabpanel-background-color: var(--hilal-glass-content-background, transparent);
+    }
+  }
 }
 
 /* stylelint-disable-next-line media-query-no-invalid */
@@ -204,6 +210,26 @@
   }
 }
 
+@media (-moz-platform: macos) {
+  :root:not([inDOMFullscreen], [privatebrowsingmode="temporary"]) #browser,
+  :root:not([inDOMFullscreen], [privatebrowsingmode="temporary"]) #tabbrowser-tabbox,
+  :root:not([inDOMFullscreen], [privatebrowsingmode="temporary"]) #tabbrowser-tabpanels,
+  :root:not([inDOMFullscreen], [privatebrowsingmode="temporary"]) .browserSidebarContainer,
+  :root:not([inDOMFullscreen], [privatebrowsingmode="temporary"]) .browserStack {
+    background: transparent !important;
+  }
+
+  :root:not([inDOMFullscreen], [privatebrowsingmode="temporary"]) .browserContainer {
+    background: transparent !important;
+    box-shadow: none !important;
+    outline-color: transparent;
+  }
+
+  :root:not([inDOMFullscreen], [privatebrowsingmode="temporary"]) browser[type="content"][transparent="true"] {
+    background: transparent !important;
+  }
+}
+
 .browserSidebarContainer {
   position: absolute;
   inset: 0;
diff --git a/widget/cocoa/VibrancyManager.mm b/widget/cocoa/VibrancyManager.mm
index 5df70a63afb2..2ad7425357c1 100644
--- a/widget/cocoa/VibrancyManager.mm
+++ b/widget/cocoa/VibrancyManager.mm
@@ -36,7 +36,7 @@ static NSVisualEffectMaterial VisualEffectMaterialForVibrancyType(
     VibrancyType aType) {
   switch (aType) {
     case VibrancyType::Sidebar:
-      return NSVisualEffectMaterialSidebar;
+      return NSVisualEffectMaterialHUDWindow;
     case VibrancyType::Titlebar:
       return NSVisualEffectMaterialTitlebar;
   }
diff --git a/widget/cocoa/nsCocoaWindow.mm b/widget/cocoa/nsCocoaWindow.mm
index 0e2fa0c1c0f7..bc525c8b636d 100644
--- a/widget/cocoa/nsCocoaWindow.mm
+++ b/widget/cocoa/nsCocoaWindow.mm
@@ -5105,8 +5105,11 @@ static unsigned int WindowMaskForBorderStyle(BorderStyle aBorderStyle) {
     mWindow.collectionBehavior = mWindow.collectionBehavior |
                                  NSWindowCollectionBehaviorMoveToActiveSpace;
   } else {
-    // Non-popup windows are always opaque.
-    mWindow.opaque = YES;
+    const bool hilalTransparentChrome = mWindowType == WindowType::TopLevel;
+    mWindow.opaque = !hilalTransparentChrome;
+    if (hilalTransparentChrome) {
+      mWindow.backgroundColor = NSColor.clearColor;
+    }
   }
 
   if (mAlwaysOnTop || mIsAlert) {
@@ -5620,7 +5623,9 @@ static void SyncPopoverBounds(NSPopover* aPopover,
     return;
   }
 
-  BOOL isTransparent = aMode == TransparencyMode::Transparent;
+  const bool hilalTransparentChrome = mWindowType == WindowType::TopLevel;
+  BOOL isTransparent =
+      hilalTransparentChrome || aMode == TransparencyMode::Transparent;
   BOOL currentTransparency = !mWindow.isOpaque;
   if (isTransparent == currentTransparency) {
     return;
