. "$topsrcdir/../mozconfigs/base"

ac_add_options --enable-linker=lld
# GitHub Actions ubuntu-latest: 4 cores, 16 GB RAM.
# Unconstrained linking OOMs the runner; cap at 3 parallel jobs.
mk_add_options MOZ_MAKE_FLAGS="-j3"