diff --git a/browser/components/customizableui/CustomizableUI.sys.mjs b/browser/components/customizableui/CustomizableUI.sys.mjs
--- a/browser/components/customizableui/CustomizableUI.sys.mjs
+++ b/browser/components/customizableui/CustomizableUI.sys.mjs
@@ -320,6 +320,11 @@ var CustomizableUIInternal = {
   initialize() {
     lazy.log.debug("Initializing");
 
+    if (!Services.prefs.getBoolPref("hilal.toolbar.initialized", false)) {
+      Services.prefs.clearUserPref("browser.uiCustomization.state");
+      Services.prefs.setBoolPref("hilal.toolbar.initialized", true);
+    }
+
     this._setAutoTouchModeDefault();
 
     lazy.AddonManagerPrivate.databaseReady.then(async () => {
