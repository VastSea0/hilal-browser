diff --git a/browser/components/tabbrowser/AsyncTabSwitcher.sys.mjs b/browser/components/tabbrowser/AsyncTabSwitcher.sys.mjs
--- a/browser/components/tabbrowser/AsyncTabSwitcher.sys.mjs
+++ b/browser/components/tabbrowser/AsyncTabSwitcher.sys.mjs
@@ -354,6 +354,9 @@ export class AsyncTabSwitcher {
   // This function is called after all the main state changes to
   // make sure we display the right tab.
   updateDisplay() {
+    if (!this.requestedTab?.linkedBrowser) {
+      return;
+    }
     let requestedTabState = this.getTabState(this.requestedTab);
     let requestedBrowser = this.requestedTab.linkedBrowser;
 
@@ -619,11 +622,15 @@ export class AsyncTabSwitcher {
     this.assert(!this.loadTimer || this.loadingTab);
     this.assert(!this.loadingTab || this.loadTimer);
 
+    if (!this.requestedTab) {
+      return;
+    }
+
     // If we're switching to a non-remote tab, there's no need to wait
     // for it to send layers to the compositor, as this will happen
     // synchronously. Clearing this here means that in the next step,
     // we can load the non-remote browser immediately.
-    if (!this.requestedTab.linkedBrowser.isRemoteBrowser) {
+    if (this.requestedTab.linkedBrowser && !this.requestedTab.linkedBrowser.isRemoteBrowser) {
       this.maybeClearLoadTimer("postActions");
     }
 
@@ -1102,9 +1109,7 @@ export class AsyncTabSwitcher {
       this.setTimer(() => this.handleEvent(event, true), 0);
       return;
     }
-    if (delayed && this.tabbrowser._switcher != this) {
-      // if we delayed processing this event, we might be out of date, in which
-      // case we drop the delayed events
+    if (this.tabbrowser._switcher !== this) {
       return;
     }
     this._processing = true;
diff --git a/browser/components/tabbrowser/Tabbrowser.sys.mjs b/browser/components/tabbrowser/Tabbrowser.sys.mjs
--- a/browser/components/tabbrowser/Tabbrowser.sys.mjs
+++ b/browser/components/tabbrowser/Tabbrowser.sys.mjs
@@ -4540,8 +4540,12 @@ export class Tabbrowser {
 
     // Related tab inherits current tab's user context unless a different
     // usercontextid is specified
-    if (userContextId == null && openerTab) {
-      userContextId = openerTab.userContextId;
+    if (userContextId == null) {
+      if (openerTab) {
+        userContextId = openerTab.userContextId || 0;
+      } else if (this.documentGlobal?.gHilalWorkspaces) {
+        userContextId = this.documentGlobal.gHilalWorkspaces.activeContainerId || 0;
+      }
     }
 
     if (!noInitialLabel) {
@@ -9356,11 +9360,11 @@ export class Tabbrowser {
   }
 
   on_activate() {
-    this.selectedTab.updateLastSeenActive();
+    this.selectedTab?.updateLastSeenActive();
   }
 
   on_deactivate() {
-    this.selectedTab.updateLastSeenActive();
+    this.selectedTab?.updateLastSeenActive();
   }
 
   on_change() {
