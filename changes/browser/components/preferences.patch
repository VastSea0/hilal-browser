diff --git a/browser/components/preferences/main.js b/browser/components/preferences/main.js
--- a/browser/components/preferences/main.js
+++ b/browser/components/preferences/main.js
@@ -13,7 +13,7 @@
 /* import-globals-from preferences.js */
 /* import-globals-from /toolkit/mozapps/preferences/fontbuilder.js */
 /* import-globals-from /browser/base/content/aboutDialog-appUpdater.js */
-/* global MozXULElement */
+/* global ContextualIdentityService, MozXULElement, Services */
 
 /**
  * @import { Setting } from "chrome://global/content/preferences/Setting.mjs"
@@ -26,6 +26,8 @@ const { Multilingual } = ChromeUtils.importESModule(
 
 ChromeUtils.defineESModuleGetters(this, {
   BackgroundUpdate: "resource://gre/modules/BackgroundUpdate.sys.mjs",
+  ContextualIdentityService:
+    "resource://gre/modules/ContextualIdentityService.sys.mjs",
   UpdateListener: "resource://gre/modules/UpdateListener.sys.mjs",
   LinkPreview: "moz-src:///browser/components/genai/LinkPreview.sys.mjs",
   MigrationUtils: "resource:///modules/MigrationUtils.sys.mjs",
@@ -1785,6 +1787,103 @@ gMainPane.initialized = new Promise(res => {
   gMainPane.setInitialized = res;
 });
 
+var gHilalPane = {
+  init() {
+    function setEventListener(aId, aEventType, aCallback) {
+      document.getElementById(aId).addEventListener(aEventType, aCallback);
+    }
+
+    const hilalWsCheckbox = document.getElementById("hilalWorkspacesEnabled");
+    const hilalWsPinnedPublicCheckbox = document.getElementById("hilalWorkspacesPinnedPublic");
+    const hilalWsGroupsPublicCheckbox = document.getElementById("hilalWorkspacesGroupsPublic");
+    const resetHilalWorkspaceContainers = () => {
+      let workspaces = [];
+      try {
+        workspaces = JSON.parse(
+          Services.prefs.getStringPref("hilal.workspaces.data", "[]")
+        );
+      } catch (e) {
+        console.warn("Failed to parse Hilal workspace data during reset", e);
+      }
+      if (!Array.isArray(workspaces)) {
+        return;
+      }
+      for (const workspace of workspaces) {
+        const containerId = Number.parseInt(workspace?.containerId, 10);
+        if (containerId) {
+          try {
+            ContextualIdentityService.remove(containerId);
+          } catch (e) {
+            console.warn("Failed to remove Hilal workspace container", e);
+          }
+        }
+      }
+    };
+
+    if (hilalWsCheckbox) {
+      hilalWsCheckbox.checked = Services.prefs.getBoolPref(
+        "hilal.workspaces.enabled",
+        true
+      );
+      hilalWsCheckbox.addEventListener("command", function () {
+        Services.prefs.setBoolPref(
+          "hilal.workspaces.enabled",
+          hilalWsCheckbox.checked
+        );
+      });
+    }
+    if (hilalWsPinnedPublicCheckbox) {
+      hilalWsPinnedPublicCheckbox.checked = Services.prefs.getBoolPref(
+        "hilal.workspaces.pinned.public",
+        true
+      );
+      hilalWsPinnedPublicCheckbox.addEventListener("command", function () {
+        Services.prefs.setBoolPref(
+          "hilal.workspaces.pinned.public",
+          hilalWsPinnedPublicCheckbox.checked
+        );
+      });
+    }
+    if (hilalWsGroupsPublicCheckbox) {
+      hilalWsGroupsPublicCheckbox.checked = Services.prefs.getBoolPref(
+        "hilal.workspaces.groups.public",
+        false
+      );
+      hilalWsGroupsPublicCheckbox.addEventListener("command", function () {
+        Services.prefs.setBoolPref(
+          "hilal.workspaces.groups.public",
+          hilalWsGroupsPublicCheckbox.checked
+        );
+      });
+    }
+    setEventListener("hilalWorkspacesReset", "command", function () {
+      if (
+        Services.prompt.confirm(
+          window,
+          "Reset Workspaces",
+          "This will delete all workspace data and cannot be undone. Continue?"
+        )
+      ) {
+        resetHilalWorkspaceContainers();
+        Services.prefs.setStringPref("hilal.workspaces.data", "[]");
+        Services.prefs.setStringPref("hilal.workspaces.active", "default");
+        Services.prefs.setBoolPref("hilal.workspaces.enabled", true);
+        Services.prefs.setBoolPref("hilal.workspaces.pinned.public", true);
+        Services.prefs.setBoolPref("hilal.workspaces.groups.public", false);
+        if (hilalWsCheckbox) {
+          hilalWsCheckbox.checked = true;
+        }
+        if (hilalWsPinnedPublicCheckbox) {
+          hilalWsPinnedPublicCheckbox.checked = true;
+        }
+        if (hilalWsGroupsPublicCheckbox) {
+          hilalWsGroupsPublicCheckbox.checked = false;
+        }
+      }
+    });
+  },
+};
+
 var {
   InternalHandlerInfoWrapper,
   getFileDisplayName,
diff --git a/browser/components/preferences/preferences.js b/browser/components/preferences/preferences.js
index 1f77010aeb8e..2729ac0a57b1 100644
--- a/browser/components/preferences/preferences.js
+++ b/browser/components/preferences/preferences.js
@@ -559,6 +559,7 @@ function init_all() {
     document.getElementById("category-general").hidden = false;
     document.getElementById("nav-separator").hidden = true;
   }
+  register_module("paneHilal", gHilalPane);
   register_module("paneHome", gHomePane);
   register_module("paneSearch", gSearchPane);
   register_module("panePrivacy", gPrivacyPane);
@@ -1170,17 +1171,15 @@ function appendSearchKeywords(aId, keywords) {
 }
 
 function maybeDisplayPoliciesNotice() {
-  if (Services.policies.status == Services.policies.ACTIVE) {
-    document
-      .getElementById("policies-container-content")
-      .removeAttribute("hidden");
+  let el = document.getElementById("policies-container-content");
+  if (el && Services.policies.status == Services.policies.ACTIVE) {
+    el.removeAttribute("hidden");
   }
 }
 
 function maybeDisplayTLSKeyLoggingNotice() {
-  if (Services.env.exists("SSLKEYLOGFILE")) {
-    document
-      .getElementById("tls-key-logging-container-content")
-      .removeAttribute("hidden");
+  let el = document.getElementById("tls-key-logging-container-content");
+  if (el && Services.env.exists("SSLKEYLOGFILE")) {
+    el.removeAttribute("hidden");
   }
 }
diff --git a/browser/components/preferences/preferences.xhtml b/browser/components/preferences/preferences.xhtml
index bb84d15bec40..f8622988a33b 100644
--- a/browser/components/preferences/preferences.xhtml
+++ b/browser/components/preferences/preferences.xhtml
@@ -38,6 +38,7 @@
   <link rel="stylesheet" href="chrome://browser/content/preferences/widgets/setting-group.css" />
   <link rel="stylesheet" href="chrome://browser/content/preferences/widgets/setting-pane.css" />
   <link rel="stylesheet" href="chrome://browser/content/preferences/widgets/setting-control.css" />
+  <link rel="stylesheet" href="chrome://browser/skin/preferences/hilal-preferences.css" />
 
   <link rel="localization" href="branding/brand.ftl"/>
   <link rel="localization" href="browser/browser.ftl"/>
@@ -127,6 +128,11 @@
         iconsrc="chrome://browser/skin/preferences/category-sync.svg"
         data-l10n-id="pane-sync-title3">
       </html:moz-page-nav-button>
+      <html:moz-page-nav-button id="category-hilal"
+        view="paneHilal"
+        iconsrc="chrome://global/skin/icons/settings.svg"
+        data-l10n-id="hilal-settings-header">
+      </html:moz-page-nav-button>
       <html:moz-page-nav-button id="category-home"
         view="paneHome"
         iconsrc="chrome://browser/skin/home.svg"
@@ -230,20 +235,6 @@
     <vbox class="main-content" flex="1" align="start">
       <vbox class="pane-container">
         <html:div id="search-container">
-          <html:nav-notice
-            id="policies-container-content"
-            hidden="true"
-            href="about:policies"
-            iconsrc="chrome://global/skin/icons/organizational-unit.svg"
-            data-l10n-id="managed-notice-nav"
-          />
-          <html:nav-notice
-            id="tls-key-logging-container-content"
-            hidden="true"
-            iconsrc="chrome://global/skin/icons/security-custom-root.svg"
-            data-l10n-id="tls-key-logging-notice-nav"
-            support-page="sslkeylogfile-warning"
-          />
           <html:moz-input-search
             id="searchInput"
             data-l10n-id="search-input-box2"
@@ -253,6 +244,7 @@
         <vbox id="mainPrefPane">
 #include searchResults.inc.xhtml
 #include main.inc.xhtml
+#include hilal.inc.xhtml
 #include home.inc.xhtml
 #include search.inc.xhtml
 #include privacy.inc.xhtml
diff --git a/browser/components/preferences/privacy.inc.xhtml b/browser/components/preferences/privacy.inc.xhtml
index 78e2e934cf33..301394391f3c 100644
--- a/browser/components/preferences/privacy.inc.xhtml
+++ b/browser/components/preferences/privacy.inc.xhtml
@@ -15,8 +15,6 @@
   <html:h1 data-l10n-id="privacy-header"/>
 </hbox>
 
-<html:setting-group hidden="true" groupid="securityPrivacyStatus" data-category="panePrivacy" data-srd-migrated="" />
-<html:setting-group hidden="true" groupid="securityPrivacyWarnings" data-category="panePrivacy" data-srd-migrated="" />
 
 <!-- Enhanced Tracking Protection Status Section -->
 <html:setting-group groupid="etpStatus" id="etpStatusCard" data-category="panePrivacy" data-subcategory="etpStatus" hidden="true" data-srd-migrated=""></html:setting-group>
