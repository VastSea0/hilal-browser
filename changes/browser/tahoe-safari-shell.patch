diff --git a/browser/app/Makefile.in b/browser/app/Makefile.in
--- a/browser/app/Makefile.in
+++ b/browser/app/Makefile.in
@@ -90,6 +90,6 @@ ifdef MOZ_UPDATER
 	cp -f '$(dist_dest)/Contents/MacOS/updater.app/Contents/MacOS/org.mozilla.updater' '$(dist_dest)/Contents/Library/LaunchServices'
 endif
 	$(MKDIR) -p '$(dist_dest)/Contents/Frameworks'
-	mv '$(dist_dest)/Contents/Resources/ChannelPrefs.framework' '$(dist_dest)/Contents/Frameworks'
+	-mv '$(dist_dest)/Contents/Resources/ChannelPrefs.framework' '$(dist_dest)/Contents/Frameworks'
 	printf APPLMOZB > '$(dist_dest)/Contents/PkgInfo'
 endif
diff --git a/browser/app/profile/firefox.js b/browser/app/profile/firefox.js
--- a/browser/app/profile/firefox.js
+++ b/browser/app/profile/firefox.js
@@ -3618,6 +3618,7 @@ pref("distribution.mozillaonline.ignore", true);
 pref("hilal.privacy.initialized", false);
 pref("hilal.bangs.custom", "[]");
 // Hilal Browser - UI Fix Integration
+pref("hilal.tahoe.enabled", true);
 pref("hilal.uifix.enabled", true);
 // Theme Default Options
 pref("toolkit.legacyUserProfileCustomizations.stylesheets", true);
diff --git a/browser/base/content/browser.xhtml b/browser/base/content/browser.xhtml
--- a/browser/base/content/browser.xhtml
+++ b/browser/base/content/browser.xhtml
@@ -152,6 +152,15 @@
   <!-- Put it at the very end to make sure it's not covered by anything. -->
   <html:div id="fullscr-toggler" hidden="hidden"/>
 
+  <svg xmlns="http://www.w3.org/2000/svg" style="position: absolute; width: 0; height: 0; pointer-events: none;">
+    <defs>
+      <filter id="liquid-glass-refraction" x="0%" y="0%" width="100%" height="100%">
+        <feTurbulence type="fractalNoise" baseFrequency="0.02 0.04" numOctaves="3" result="noise" />
+        <feDisplacementMap in="SourceGraphic" in2="noise" scale="10" xChannelSelector="R" yChannelSelector="G" />
+      </filter>
+    </defs>
+  </svg>
+
   <script src="chrome://browser/content/hilal/HilalKeyboardShortcuts.js"></script>
   <script src="chrome://browser/content/hilal/HilalWelcome.js"></script>
   <script src="chrome://browser/content/hilal/HilalWorkspaces.js"></script>
diff --git a/browser/base/jar.mn b/browser/base/jar.mn
--- a/browser/base/jar.mn
+++ b/browser/base/jar.mn
@@ -90,6 +90,8 @@ browser.jar:
         content/browser/hilal/HilalBoosts.js                (content/hilal/HilalBoosts.js)
         content/browser/hilal/HilalBoostsActorParent.sys.mjs (content/hilal/HilalBoostsActorParent.sys.mjs)
         content/browser/hilal/HilalBoostsActorChild.sys.mjs  (content/hilal/HilalBoostsActorChild.sys.mjs)
+        content/browser/hilal/HilalTahoeParent.sys.mjs      (content/hilal/HilalTahoeParent.sys.mjs)
+        content/browser/hilal/HilalTahoeChild.sys.mjs       (content/hilal/HilalTahoeChild.sys.mjs)
         content/browser/global-scripts.js                   (content/global-scripts.js)
         content/browser/main-popupset.js                    (content/main-popupset.js)
 *       content/browser/pageinfo/pageInfo.xhtml             (content/pageinfo/pageInfo.xhtml)
diff --git a/browser/components/customizableui/CustomizableUI.sys.mjs b/browser/components/customizableui/CustomizableUI.sys.mjs
--- a/browser/components/customizableui/CustomizableUI.sys.mjs
+++ b/browser/components/customizableui/CustomizableUI.sys.mjs
@@ -37,6 +37,18 @@ const kPrefCustomizationHorizontalTabsBackup =
   "browser.uiCustomization.horizontalTabsBackup";
 const kPrefCustomizationNavBarWhenVerticalTabs =
   "browser.uiCustomization.navBarWhenVerticalTabs";
+const kHilalHorizontalTabToolbarWidgets = ["new-tab-button"];
+const kHilalHorizontalTabToolbarExcludedWidgets = [
+  "firefox-view-button",
+  "alltabs-button",
+];
+const kHilalHorizontalTabstripDefaultPlacements = ["tabbrowser-tabs"];
+const kHilalDefaultTabstripPlacements = [
+  "firefox-view-button",
+  "tabbrowser-tabs",
+  "new-tab-button",
+  "alltabs-button",
+];
 const kPrefCustomizationAutoAdd = "browser.uiCustomization.autoAdd";
 const kPrefCustomizationDebug = "browser.uiCustomization.debug";
 const kPrefDrawInTitlebar = "browser.tabs.inTitlebar";
@@ -251,6 +263,19 @@ XPCOMUtils.defineLazyPreferenceGetter(
   }
 );
 
+XPCOMUtils.defineLazyPreferenceGetter(
+  lazy,
+  "verticalTabsShowHorizontalPref",
+  "sidebar.verticalTabs.showHorizontal",
+  false,
+  (pref, oldVal, newVal) => {
+    lazy.log.debug(
+      `sidebar.verticalTabs.showHorizontal change handler, calling updateTabStripOrientation with value: ${newVal}`
+    );
+    CustomizableUIInternal.updateTabStripOrientation();
+  }
+);
+
 XPCOMUtils.defineLazyPreferenceGetter(
   lazy,
   "horizontalPlacementsPref",
@@ -357,6 +382,9 @@ var CustomizableUIInternal = {
       false
     );
 
+    let useHilalHorizontalTabsToolbar =
+      Services.prefs.getBoolPref("sidebar.verticalTabs", false) &&
+      Services.prefs.getBoolPref("sidebar.verticalTabs.showHorizontal", false);
     let navbarPlacements = [
       lazy.sidebarRevampEnabled ? "sidebar-button" : null,
       "back-button",
@@ -375,6 +403,14 @@ var CustomizableUIInternal = {
       "fxa-toolbar-menu-button",
       lazy.resetPBMToolbarButtonEnabled ? "reset-pbm-toolbar-button" : null,
     ].filter(name => name);
+    if (useHilalHorizontalTabsToolbar) {
+      let newTabIndex = navbarPlacements.indexOf("downloads-button");
+      navbarPlacements.splice(
+        newTabIndex >= 0 ? newTabIndex + 1 : navbarPlacements.length,
+        0,
+        "new-tab-button"
+      );
+    }
 
     this.registerArea(
       CustomizableUI.AREA_NAVBAR,
@@ -407,12 +443,9 @@ var CustomizableUIInternal = {
       CustomizableUI.AREA_TABSTRIP,
       {
         type: CustomizableUI.TYPE_TOOLBAR,
-        defaultPlacements: [
-          "firefox-view-button",
-          "tabbrowser-tabs",
-          "new-tab-button",
-          "alltabs-button",
-        ],
+        defaultPlacements: useHilalHorizontalTabsToolbar
+          ? kHilalHorizontalTabstripDefaultPlacements
+          : kHilalDefaultTabstripPlacements,
         verticalTabsDefaultPlacements: [],
         defaultCollapsed: null,
       },
@@ -445,6 +478,11 @@ var CustomizableUIInternal = {
     // we'll enable revamp rather than disable vertical tabs.
     this.reconcileSidebarPrefs(kPrefSidebarVerticalTabsEnabled);
 
+    // Force initialization of lazy preference getters so their observers are registered:
+    lazy.verticalTabsPref;
+    lazy.verticalTabsShowHorizontalPref;
+
+    this.updateHilalHorizontalTabToolbarDefaults();
     this.initializeForTabsOrientation(CustomizableUI.verticalTabsEnabled);
 
     Services.obs.addObserver(this, "browser-set-toolbar-visibility");
@@ -4466,10 +4504,17 @@ var CustomizableUIInternal = {
    */
   reset() {
     gResetting = true;
-    // CUI reset also implies resetting verticalTabs back to false.
-    // We do this before the rest of the reset so widgets are reset to their non-vertical
-    // positions.
-    Services.prefs.setBoolPref("sidebar.verticalTabs", false);
+    if (Services.prefs.getBoolPref("hilal.tahoe.enabled", true)) {
+      Services.prefs.setStringPref("sidebar.visibility", "hide-sidebar");
+      Services.prefs.setBoolPref("sidebar.verticalTabs.showHorizontal", true);
+      Services.prefs.setBoolPref("sidebar.verticalTabs", true);
+    } else {
+      // CUI reset also implies resetting verticalTabs back to false.
+      // We do this before the rest of the reset so widgets are reset to their non-vertical
+      // positions.
+      Services.prefs.setBoolPref("sidebar.verticalTabs", false);
+    }
+    this.updateHilalHorizontalTabToolbarDefaults();
     this._resetUIState();
 
     // Rebuild each registered area (across windows) to reflect the state that
@@ -5075,6 +5120,12 @@ var CustomizableUIInternal = {
 
     if (aTopic === "nsPref:changed") {
       this.reconcileSidebarPrefs(aData);
+      if (
+        aData === kPrefSidebarVerticalTabsEnabled ||
+        aData === "sidebar.verticalTabs.showHorizontal"
+      ) {
+        this.updateHilalHorizontalTabToolbarDefaults();
+      }
     }
   },
 
@@ -5094,14 +5145,19 @@ var CustomizableUIInternal = {
         "initializeForTabsOrientation, savedPlacements",
         savedPlacements
       );
-      if (savedPlacements.length) {
+      let isCurrentlyVertical =
+        gSavedState?.placements?.[
+          CustomizableUI.AREA_VERTICAL_TABSTRIP
+        ]?.includes("tabbrowser-tabs");
+      if (savedPlacements.length || isCurrentlyVertical) {
         // We're startup up with horizontal tabs, but there are saved placements for the
-        // horizontal tab strip, so its possible the verticalTabs pref was updated outside
-        // of normal use. Make sure to restore those tabstrip widget placements
+        // horizontal tab strip, or the database placements show it is currently vertical.
+        // Make sure to restore those tabstrip widget placements.
         this.restoreSavedHorizontalTabStripState(savedPlacements, true);
       } else {
         // This is the default state and normal initialization will do everything necessary
       }
+      this.updateHilalHorizontalTabToolbarState(true);
       gCurrentVerticalTabs = false;
       return;
     }
@@ -5285,6 +5341,9 @@ var CustomizableUIInternal = {
       return;
     }
     let toVertical = CustomizableUI.verticalTabsEnabled;
+    if (!toVertical) {
+      this.updateHilalHorizontalTabToolbarState();
+    }
     if (toVertical === gCurrentVerticalTabs) {
       lazy.log.debug("early return as the value hasn't changed");
       return;
@@ -5378,6 +5437,7 @@ var CustomizableUIInternal = {
       this.saveNavBarWhenVerticalTabsState();
       // We're switching to vertical in this session; pull saved state from pref and update placements
       this.restoreSavedHorizontalTabStripState();
+      this.updateHilalHorizontalTabToolbarState();
     }
     // Give the sidebar a chance to adjust before we show/hide the toolbars
     lazy.log.debug("CustomizableUI notifying tabstrip-orientation-change");
@@ -5396,6 +5456,96 @@ var CustomizableUIInternal = {
       win.TabBarVisibility.update(true);
     }
   },
+
+  updateHilalHorizontalTabToolbarDefaults() {
+    if (
+      !gAreas.get(CustomizableUI.AREA_TABSTRIP) ||
+      !gAreas.get(CustomizableUI.AREA_NAVBAR)
+    ) {
+      return;
+    }
+
+    let useHilalHorizontalTabsToolbar =
+      lazy.verticalTabsPref && lazy.verticalTabsShowHorizontalPref;
+
+    let tabstripProps = gAreas.get(CustomizableUI.AREA_TABSTRIP);
+    tabstripProps.set(
+      "defaultPlacements",
+      useHilalHorizontalTabsToolbar
+        ? [...kHilalHorizontalTabstripDefaultPlacements]
+        : [...kHilalDefaultTabstripPlacements]
+    );
+
+    let navbarProps = gAreas.get(CustomizableUI.AREA_NAVBAR);
+    let navbarDefaults = navbarProps
+      .get("defaultPlacements")
+      .filter(
+        id =>
+          !kHilalHorizontalTabToolbarWidgets.includes(id) &&
+          !kHilalHorizontalTabToolbarExcludedWidgets.includes(id)
+      );
+    if (useHilalHorizontalTabsToolbar) {
+      let insertIndex = navbarDefaults.indexOf("downloads-button");
+      navbarDefaults.splice(
+        insertIndex >= 0 ? insertIndex + 1 : navbarDefaults.length,
+        0,
+        ...kHilalHorizontalTabToolbarWidgets
+      );
+    }
+    navbarProps.set("defaultPlacements", navbarDefaults);
+  },
+
+  updateHilalHorizontalTabToolbarState(isInitializing = false) {
+    if (!lazy.verticalTabsPref || !lazy.verticalTabsShowHorizontalPref) {
+      return;
+    }
+
+    if (
+      !gPlacements.get(CustomizableUI.AREA_TABSTRIP) ||
+      !gPlacements.get(CustomizableUI.AREA_NAVBAR)
+    ) {
+      return;
+    }
+
+    let insertIndex = gPlacements
+      .get(CustomizableUI.AREA_NAVBAR)
+      .indexOf("downloads-button");
+    insertIndex =
+      insertIndex >= 0
+        ? insertIndex + 1
+        : gPlacements.get(CustomizableUI.AREA_NAVBAR).length;
+
+    this.updateHilalHorizontalTabToolbarDefaults();
+    CustomizableUI.beginBatchUpdate();
+    try {
+      for (let widgetId of kHilalHorizontalTabToolbarExcludedWidgets) {
+        let placement = this.getPlacementOfWidget(widgetId, false, true);
+        if (placement?.area === CustomizableUI.AREA_NAVBAR) {
+          this.removeWidgetFromArea(widgetId);
+        }
+      }
+
+      for (let widgetId of kHilalHorizontalTabToolbarWidgets) {
+        let navbarPlacements = gPlacements.get(CustomizableUI.AREA_NAVBAR);
+        let placement = this.getPlacementOfWidget(widgetId, false, true);
+        let currentIndex = navbarPlacements.indexOf(widgetId);
+        if (
+          placement?.area !== CustomizableUI.AREA_NAVBAR ||
+          currentIndex !== insertIndex
+        ) {
+          this.addWidgetToArea(
+            widgetId,
+            CustomizableUI.AREA_NAVBAR,
+            insertIndex,
+            isInitializing
+          );
+        }
+        insertIndex++;
+      }
+    } finally {
+      CustomizableUI.endBatchUpdate();
+    }
+  },
 };
 Object.freeze(CustomizableUIInternal);
 
@@ -5500,7 +5650,7 @@ export var CustomizableUI = {
   },
 
   get verticalTabsEnabled() {
-    return lazy.verticalTabsPref;
+    return lazy.verticalTabsPref && !lazy.verticalTabsShowHorizontalPref;
   },
 
   /**
diff --git a/browser/components/preferences/main.js b/browser/components/preferences/main.js
--- a/browser/components/preferences/main.js
+++ b/browser/components/preferences/main.js
@@ -1796,8 +1796,10 @@ var gHilalPane = {
     const hilalWsCheckbox = document.getElementById("hilalWorkspacesEnabled");
     const hilalWsPinnedPublicCheckbox = document.getElementById("hilalWorkspacesPinnedPublic");
     const hilalWsGroupsPublicCheckbox = document.getElementById("hilalWorkspacesGroupsPublic");
+    const hilalUrlbarShowWorkspaceNameCheckbox = document.getElementById("hilalUrlbarShowWorkspaceName");
     const hilalSidebarCompactCheckbox = document.getElementById("hilalSidebarCompactEnabled");
     const hilalSidebarCompactHideToolbarCheckbox = document.getElementById("hilalSidebarCompactHideToolbar");
+    const hilalSidebarShowHorizontalCheckbox = document.getElementById("hilalSidebarShowHorizontalTabs");
     const hilalPrivacyLevelRadioGroup = /** @type {HTMLElement|null} */ (
       document.getElementById("hilalPrivacyLevel")
     );
@@ -1863,6 +1865,18 @@ var gHilalPane = {
         );
       });
     }
+    if (hilalUrlbarShowWorkspaceNameCheckbox) {
+      hilalUrlbarShowWorkspaceNameCheckbox.checked = Services.prefs.getBoolPref(
+        "hilal.urlbar.showWorkspaceName",
+        false
+      );
+      hilalUrlbarShowWorkspaceNameCheckbox.addEventListener("command", function () {
+        Services.prefs.setBoolPref(
+          "hilal.urlbar.showWorkspaceName",
+          hilalUrlbarShowWorkspaceNameCheckbox.checked
+        );
+      });
+    }
     if (hilalSidebarCompactCheckbox) {
       hilalSidebarCompactCheckbox.checked = Services.prefs.getBoolPref(
         "hilal.compact.enabled",
@@ -1887,6 +1901,18 @@ var gHilalPane = {
         );
       });
     }
+    if (hilalSidebarShowHorizontalCheckbox) {
+      hilalSidebarShowHorizontalCheckbox.checked = Services.prefs.getBoolPref(
+        "sidebar.verticalTabs.showHorizontal",
+        false
+      );
+      hilalSidebarShowHorizontalCheckbox.addEventListener("command", function () {
+        Services.prefs.setBoolPref(
+          "sidebar.verticalTabs.showHorizontal",
+          hilalSidebarShowHorizontalCheckbox.checked
+        );
+      });
+    }
 
     const hilalSidebarFooterCheckboxes = [
       ["hilalSidebarFooterBookmarks", "hilal.sidebar.footer.bookmarks", false],
@@ -2106,6 +2132,7 @@ var gHilalPane = {
         Services.prefs.setBoolPref("hilal.workspaces.enabled", true);
         Services.prefs.setBoolPref("hilal.workspaces.pinned.public", true);
         Services.prefs.setBoolPref("hilal.workspaces.groups.public", false);
+        Services.prefs.setBoolPref("hilal.urlbar.showWorkspaceName", false);
         Services.prefs.setBoolPref("hilal.compact.enabled", false);
         Services.prefs.setBoolPref("hilal.compact.hide_toolbox", true);
         Services.prefs.setStringPref("hilal.privacy.level", "standard");
@@ -2118,18 +2145,28 @@ var gHilalPane = {
         if (hilalWsGroupsPublicCheckbox) {
           hilalWsGroupsPublicCheckbox.checked = false;
         }
+        if (hilalUrlbarShowWorkspaceNameCheckbox) {
+          hilalUrlbarShowWorkspaceNameCheckbox.checked = false;
+        }
         if (hilalSidebarCompactCheckbox) {
           hilalSidebarCompactCheckbox.checked = false;
         }
         if (hilalSidebarCompactHideToolbarCheckbox) {
           hilalSidebarCompactHideToolbarCheckbox.checked = true;
         }
+        const hilalTahoeCheckbox = document.getElementById("hilalTahoeEnabled");
+        if (hilalTahoeCheckbox) {
+          hilalTahoeCheckbox.checked = true;
+        }
         if (hilalPrivacyLevelRadioGroup) {
           hilalPrivacyLevelRadioGroup.value = "standard";
         }
       }
     });
 
+    const hilalTahoeCheckbox = /** @type {HTMLInputElement|null} */ (
+      document.getElementById("hilalTahoeEnabled")
+    );
     const hilalUiFixCheckbox = /** @type {HTMLInputElement|null} */ (
       document.getElementById("hilalUiFixEnabled")
     );
@@ -2183,6 +2220,19 @@ var gHilalPane = {
       }
     };
 
+    if (hilalTahoeCheckbox) {
+      hilalTahoeCheckbox.checked = Services.prefs.getBoolPref(
+        "hilal.tahoe.enabled",
+        true
+      );
+      hilalTahoeCheckbox.addEventListener("command", function () {
+        Services.prefs.setBoolPref(
+          "hilal.tahoe.enabled",
+          hilalTahoeCheckbox.checked
+        );
+      });
+    }
+
     if (hilalUiFixCheckbox) {
       hilalUiFixCheckbox.checked = Services.prefs.getBoolPref(
         "hilal.uifix.enabled",
diff --git a/browser/components/sidebar/SidebarManager.sys.mjs b/browser/components/sidebar/SidebarManager.sys.mjs
--- a/browser/components/sidebar/SidebarManager.sys.mjs
+++ b/browser/components/sidebar/SidebarManager.sys.mjs
@@ -236,6 +236,15 @@ class SidebarManager extends EventTarget {
       }
       Services.prefs.setStringPref(VISIBILITY_SETTING_PREF, "hide-sidebar");
     } else if (resetVisibility) {
+      if (
+        Services.prefs.getBoolPref(
+          "sidebar.verticalTabs.showHorizontal",
+          false
+        )
+      ) {
+        Services.prefs.setStringPref(VISIBILITY_SETTING_PREF, "hide-sidebar");
+        return;
+      }
       if (this.#savedVisibility) {
         Services.prefs.setStringPref(
           VISIBILITY_SETTING_PREF,
diff --git a/browser/components/sidebar/browser-sidebar.js b/browser/components/sidebar/browser-sidebar.js
--- a/browser/components/sidebar/browser-sidebar.js
+++ b/browser/components/sidebar/browser-sidebar.js
@@ -574,6 +574,57 @@ var SidebarController = {
     }
 
     HilalCompactMode.init();
+
+    try {
+      ChromeUtils.registerWindowActor("HilalTahoe", {
+        parent: {
+          esModuleURI:
+            "chrome://browser/content/hilal/HilalTahoeParent.sys.mjs",
+        },
+        child: {
+          esModuleURI: "chrome://browser/content/hilal/HilalTahoeChild.sys.mjs",
+          events: {
+            DOMDocElementInserted: {},
+            DOMContentLoaded: {},
+            pageshow: {},
+            scroll: { capture: true },
+          },
+        },
+        allFrames: false,
+        remoteTypes: ["web", "file", "privilegedabout", "parent"],
+      });
+    } catch (e) {
+      // Already registered
+    }
+
+    this._tabSelectListener = () => {
+      this.updateTahoeOffsets();
+    };
+    window.gBrowser.tabContainer.addEventListener(
+      "TabSelect",
+      this._tabSelectListener
+    );
+
+    this._tahoeGeometryListener = () => {
+      this.scheduleTahoeGeometryUpdate();
+    };
+    window.addEventListener("resize", this._tahoeGeometryListener);
+    if (typeof ResizeObserver == "function") {
+      this._tahoeGeometryResizeObserver = new ResizeObserver(
+        this._tahoeGeometryListener
+      );
+      for (let id of [
+        "navigator-toolbox",
+        "tabbrowser-tabbox",
+        "sidebar-main",
+      ]) {
+        let element = document.getElementById(id);
+        if (element) {
+          this._tahoeGeometryResizeObserver.observe(element);
+        }
+      }
+    }
+    this.scheduleTahoeGeometryUpdate();
   },
 
   uninit() {
@@ -608,6 +659,31 @@ var SidebarController = {
     delete this._aiWindowObserverAdded;
     delete this._windowRestoredObserverAdded;
 
+    if (this._tabSelectListener) {
+      window.gBrowser.tabContainer.removeEventListener(
+        "TabSelect",
+        this._tabSelectListener
+      );
+      this._tabSelectListener = null;
+    }
+    if (this._tahoeGeometryListener) {
+      window.removeEventListener("resize", this._tahoeGeometryListener);
+      this._tahoeGeometryListener = null;
+    }
+    if (this._tahoeGeometryResizeObserver) {
+      this._tahoeGeometryResizeObserver.disconnect();
+      this._tahoeGeometryResizeObserver = null;
+    }
+    if (this._tahoeGeometryFrame) {
+      cancelAnimationFrame(this._tahoeGeometryFrame);
+      this._tahoeGeometryFrame = 0;
+    }
+    if (this._tahoeReflectionFrame) {
+      cancelAnimationFrame(this._tahoeReflectionFrame);
+      this._tahoeReflectionFrame = 0;
+    }
+    this.clearTahoeScrollReflection();
+
     CustomizableUI.removeListener(this);
 
     if (this._observer) {
@@ -2071,6 +2147,7 @@ var SidebarController = {
       this.updateToolbarButton();
       this.dismissSidebarBadge(commandID);
 
+      this.updateTahoeOffsets(true);
       this._fireFocusedEvent();
       return true;
     });
@@ -2095,6 +2172,7 @@ var SidebarController = {
     }
     return this._show(commandID).then(() => {
       this._loadSidebarExtension(commandID);
+      this.updateTahoeOffsets(true);
       return true;
     });
   },
@@ -2278,6 +2356,7 @@ var SidebarController = {
       updateToggleControlLabel(triggerNode);
     }
     this.updateToolbarButton();
+    this.updateTahoeOffsets(false);
   },
 
   /**
@@ -2374,7 +2453,9 @@ var SidebarController = {
   },
 
   toggleTabstrip() {
-    let toVerticalTabs = CustomizableUI.verticalTabsEnabled;
+    let toVerticalTabs =
+      CustomizableUI.verticalTabsEnabled &&
+      !Services.prefs.getBoolPref("sidebar.verticalTabs.showHorizontal", false);
     let tabStrip = gBrowser.tabContainer;
     let arrowScrollbox = tabStrip.arrowScrollbox;
     let currentScrollOrientation = arrowScrollbox.getAttribute("orient");
@@ -2414,6 +2495,233 @@ var SidebarController = {
     }
   },
 
+  updateTahoeOffsets() {
+    this.scheduleTahoeGeometryUpdate();
+    let browser = window.gBrowser.selectedBrowser;
+    if (browser && browser.browsingContext) {
+      try {
+        let actor =
+          browser.browsingContext.currentWindowGlobal?.getActor("HilalTahoe");
+        if (actor) {
+          actor.sendAsyncMessage("HilalTahoe:UpdateOffsets", {});
+        }
+      } catch (e) {
+        // Ignore "doesn't match remote type" errors
+      }
+    }
+  },
+
+  scheduleTahoeGeometryUpdate() {
+    if (this._tahoeGeometryFrame) {
+      return;
+    }
+    this._tahoeGeometryFrame = requestAnimationFrame(() => {
+      this._tahoeGeometryFrame = 0;
+      this.updateTahoeGeometry();
+    });
+  },
+
+  updateTahoeGeometry() {
+    let tabbox = document.getElementById("tabbrowser-tabbox");
+    if (!tabbox) {
+      return;
+    }
+
+    let rect = tabbox.getBoundingClientRect();
+    let top = Math.max(0, Math.round(rect.top));
+    let bottom = Math.max(0, Math.round(window.innerHeight - rect.bottom));
+    let height = Math.max(0, Math.round(rect.height));
+    let docEl = document.documentElement;
+    docEl.style.setProperty("--hilal-safari-content-top", `${top}px`);
+    docEl.style.setProperty("--hilal-safari-content-bottom", `${bottom}px`);
+    docEl.style.setProperty("--hilal-safari-content-height", `${height}px`);
+  },
+
+  updateTahoeScrollReflection(scrollState, browsingContext) {
+    let browser = window.gBrowser?.selectedBrowser;
+    if (!browser || browser.browsingContext !== browsingContext) {
+      return;
+    }
+    if (!this.isTahoeSafariLayoutActive()) {
+      this.clearTahoeScrollReflection();
+      return;
+    }
+
+    let top = this.readTahoeContentTop();
+    let scrollY = Math.max(0, Math.round(scrollState?.scrollY || 0));
+    let pageBackgroundTop = Math.max(0, top - Math.min(top, scrollY));
+    document.documentElement.style.setProperty(
+      "--hilal-safari-page-bg-top",
+      `${pageBackgroundTop}px`
+    );
+
+    this._lastTahoeScrollState = scrollState;
+    this._lastTahoeScrollBrowsingContext = browsingContext;
+    this._tahoeReflectionDirty = true;
+    this.scheduleTahoeScrollReflectionUpdate();
+  },
+
+  scheduleTahoeScrollReflectionUpdate() {
+    if (this._tahoeReflectionFrame || this._tahoeReflectionPending) {
+      return;
+    }
+    this._tahoeReflectionFrame = requestAnimationFrame(() => {
+      this._tahoeReflectionFrame = 0;
+      this.updateTahoeScrollReflectionCanvas();
+    });
+  },
+
+  async updateTahoeScrollReflectionCanvas() {
+    if (this._tahoeReflectionPending) {
+      return;
+    }
+
+    this._tahoeReflectionPending = true;
+    this._tahoeReflectionDirty = false;
+
+    try {
+      let scrollState = this._lastTahoeScrollState;
+      let browser = window.gBrowser?.selectedBrowser;
+      if (
+        !scrollState ||
+        !browser ||
+        browser.browsingContext !== this._lastTahoeScrollBrowsingContext ||
+        !this.isTahoeSafariLayoutActive()
+      ) {
+        this.clearTahoeScrollReflection();
+        return;
+      }
+
+      let top = this.readTahoeContentTop();
+      let scrollY = Math.max(0, Math.round(scrollState.scrollY || 0));
+      let captureHeight = Math.min(top, scrollY);
+      if (captureHeight <= 0) {
+        this.clearTahoeScrollReflection();
+        return;
+      }
+
+      let tabbox = document.getElementById("tabbrowser-tabbox");
+      let tabboxRect = tabbox.getBoundingClientRect();
+      let width = Math.max(1, Math.round(tabboxRect.width));
+      let left = Math.max(0, Math.round(tabboxRect.left));
+      let pageBackground = getComputedStyle(tabbox).backgroundColor;
+      // The reflection is heavily blurred, so a 1x snapshot keeps scrolling responsive.
+      let scale = 1;
+      let canvas = this.ensureTahoeScrollReflectionCanvas();
+      let canvasWidth = Math.max(1, Math.round(width * scale));
+      let canvasHeight = Math.max(1, Math.round(top * scale));
+      if (canvas.width !== canvasWidth) {
+        canvas.width = canvasWidth;
+      }
+      if (canvas.height !== canvasHeight) {
+        canvas.height = canvasHeight;
+      }
+      canvas.style.insetInlineStart = `${left}px`;
+      canvas.style.inlineSize = `${width}px`;
+      canvas.style.blockSize = `${top}px`;
+
+      let captureWidth = Math.max(
+        1,
+        Math.round(scrollState.innerWidth || browser.clientWidth || width)
+      );
+      let captureX = Math.max(0, Math.round(scrollState.scrollX || 0));
+      let captureY = Math.max(0, scrollY - captureHeight);
+      let serial = (this._tahoeReflectionSerial || 0) + 1;
+      this._tahoeReflectionSerial = serial;
+      let image = await browser.drawSnapshot(
+        captureX,
+        captureY,
+        captureWidth,
+        captureHeight,
+        scale,
+        "transparent",
+        false
+      );
+      if (
+        !image ||
+        serial !== this._tahoeReflectionSerial ||
+        browser !== window.gBrowser?.selectedBrowser
+      ) {
+        image?.close?.();
+        return;
+      }
+
+      let dstY = Math.round((top - captureHeight) * scale);
+      let dstHeight = Math.round(captureHeight * scale);
+      let ctx = canvas.getContext("2d");
+      ctx.clearRect(0, 0, canvas.width, canvas.height);
+      if (pageBackground.startsWith("rgb")) {
+        ctx.fillStyle = pageBackground;
+        ctx.fillRect(0, 0, canvas.width, canvas.height);
+      }
+      ctx.drawImage(image, 0, dstY, canvas.width, dstHeight);
+      image.close?.();
+      document.documentElement.toggleAttribute(
+        "hilal-safari-reflection-active",
+        true
+      );
+    } catch (e) {
+      this.clearTahoeScrollReflection();
+    } finally {
+      this._tahoeReflectionPending = false;
+      if (this._tahoeReflectionDirty) {
+        this.scheduleTahoeScrollReflectionUpdate();
+      }
+    }
+  },
+
+  isTahoeSafariLayoutActive() {
+    if (!Services.prefs.getBoolPref("hilal.tahoe.enabled", true)) {
+      return false;
+    }
+    let docEl = document.documentElement;
+    return (
+      !docEl.hasAttribute("macOSNativeFullscreen") &&
+      !docEl.hasAttribute("customizing") &&
+      docEl.getAttribute("sizemode") !== "fullscreen" &&
+      !!document.querySelector(
+        "#sidebar-main[hilal-sidebar]:not([hidden]):not([sidebar-positionend])"
+      )
+    );
+  },
+
+  readTahoeContentTop() {
+    let value = parseFloat(
+      getComputedStyle(document.documentElement).getPropertyValue(
+        "--hilal-safari-content-top"
+      )
+    );
+    if (!Number.isFinite(value) || value <= 0) {
+      value = parseFloat(
+        getComputedStyle(document.documentElement).getPropertyValue(
+          "--hilal-safari-toolbox-height"
+        )
+      );
+    }
+    return Math.max(1, Math.round(value || 94));
+  },
+
+  ensureTahoeScrollReflectionCanvas() {
+    let canvas = document.getElementById("hilal-safari-scroll-reflection");
+    if (canvas) {
+      return canvas;
+    }
+
+    canvas = document.createElementNS("http://www.w3.org/1999/xhtml", "canvas");
+    canvas.id = "hilal-safari-scroll-reflection";
+    canvas.className = "hilal-safari-scroll-reflection";
+    document.getElementById("hilal-browser-background")?.appendChild(canvas);
+    return canvas;
+  },
+
+  clearTahoeScrollReflection() {
+    let docEl = document.documentElement;
+    docEl.toggleAttribute("hilal-safari-reflection-active", false);
+    docEl.style.removeProperty("--hilal-safari-page-bg-top");
+    let canvas = document.getElementById("hilal-safari-scroll-reflection");
+    canvas?.getContext("2d")?.clearRect(0, 0, canvas.width, canvas.height);
+  },
+
   debouncedMouseEnter() {
     const contentArea = document.getElementById("tabbrowser-tabbox");
     this._box.toggleAttribute("sidebar-launcher-hovered", true);
@@ -2933,9 +3241,10 @@ var HilalCompactMode = {
       false
     );
     const isSidebarActive = workspacesEnabled || verticalTabsEnabled;
-    const isWelcomeStage = document.documentElement.hasAttribute("hilal-welcome-stage");
+    const isWelcomeStage = document.documentElement.hasAttribute(
+      "hilal-welcome-stage"
+    );
     const actualCompact = compact;
-
     const compactSidebar = actualCompact && isSidebarActive;
     const hideToolbox =
       actualCompact && Services.prefs.getBoolPref(this.HIDE_TOOLBOX_PREF, true);
diff --git a/browser/components/sidebar/sidebar-main.css b/browser/components/sidebar/sidebar-main.css
--- a/browser/components/sidebar/sidebar-main.css
+++ b/browser/components/sidebar/sidebar-main.css
@@ -336,7 +336,10 @@
   display: flex;
   justify-content: center;
   position: relative;
-  transition: background 0.15s ease, border-color 0.15s ease, transform 0.1s ease;
+  transition:
+    background 0.15s ease,
+    border-color 0.15s ease,
+    transform 0.1s ease;
 }
 
 .hilal-pinned-card:hover {
@@ -547,6 +550,510 @@
   text-align: start;
 }
 
+@media -moz-pref("hilal.tahoe.enabled") and (-moz-platform: macos) {
+  :host([hilal-sidebar]) {
+    --hilal-sidebar-muted: color-mix(in srgb, var(--hilal-sidebar-text) 62%, transparent);
+    --hilal-sidebar-faint: light-dark(rgba(0, 0, 0, 0.04), rgba(255, 255, 255, 0.05));
+    --hilal-sidebar-hover: light-dark(rgba(0, 0, 0, 0.05), rgba(255, 255, 255, 0.06));
+    --hilal-sidebar-highlight: light-dark(rgba(0, 0, 0, 0.08), rgba(255, 255, 255, 0.08));
+    --hilal-sidebar-highlight-text: var(--hilal-sidebar-text);
+    --hilal-sidebar-blue: light-dark(#0a84ff, #64a7ff);
+    --hilal-sidebar-border: transparent;
+    inline-size: var(--hilal-safari-sidebar-width, 260px) !important;
+    max-inline-size: var(--hilal-safari-sidebar-width, 260px) !important;
+    min-inline-size: var(--hilal-safari-sidebar-width, 260px) !important;
+  }
+
+  :host([hilal-sidebar]:not([vertical-tabs])) {
+    inline-size: var(--hilal-safari-sidebar-width, 260px) !important;
+    max-inline-size: var(--hilal-safari-sidebar-width, 260px) !important;
+    min-inline-size: var(--hilal-safari-sidebar-width, 260px) !important;
+  }
+
+  .hilal-sidebar-layout {
+    background: transparent;
+    padding-block-start: var(--hilal-sidebar-top-padding, 82px);
+  }
+
+  .hilal-sidebar-layout::before {
+    border-bottom: 0;
+  }
+
+  .hilal-workspace-rail {
+    display: none;
+  }
+
+  .hilal-workspace-list {
+    gap: 9px;
+  }
+
+  .hilal-workspace-button,
+  #hilal-workspace-add {
+    block-size: 36px;
+    border-radius: 11px;
+    flex-basis: 36px;
+    inline-size: 36px;
+    transition:
+      background-color 0.16s ease,
+      border-color 0.16s ease,
+      transform 0.12s ease;
+  }
+
+  .hilal-workspace-button:hover,
+  #hilal-workspace-add:hover {
+    transform: translateY(-0.5px);
+  }
+
+  .hilal-workspace-active {
+    background: var(--hilal-sidebar-highlight);
+    border-color: color-mix(in srgb, var(--hilal-sidebar-blue) 24%, transparent);
+    box-shadow:
+      0 1px 0 light-dark(rgba(255, 255, 255, 0.5), rgba(255, 255, 255, 0.08)) inset,
+      0 6px 16px color-mix(in srgb, var(--hilal-sidebar-blue) 18%, transparent);
+  }
+
+  #hilal-workspace-add {
+    border-style: solid;
+    color: var(--hilal-sidebar-muted);
+  }
+
+  .hilal-sidebar-panel {
+    padding: 0 13px 0 26px;
+  }
+
+  .hilal-panel-header {
+    position: absolute;
+    top: 22px;
+    right: 12px;
+    z-index: 12;
+    justify-content: flex-end;
+    gap: 6px;
+    min-block-size: 32px;
+    margin: 0;
+  }
+
+  .hilal-panel-workspace-icon,
+  .hilal-panel-title {
+    display: none;
+  }
+
+  .hilal-icon-button {
+    block-size: 30px;
+    border-radius: 999px;
+    inline-size: 30px;
+  }
+
+  #hilal-sidebar-search {
+    background: var(--hilal-sidebar-faint);
+    border-color: color-mix(in srgb, var(--hilal-sidebar-text) 10%, transparent);
+    border-radius: 999px;
+    min-block-size: 30px;
+    padding-inline: 10px;
+  }
+
+  #hilal-sidebar-search:focus {
+    border-color: color-mix(in srgb, var(--hilal-sidebar-blue) 34%, transparent);
+    box-shadow: 0 0 0 3px color-mix(in srgb, var(--hilal-sidebar-blue) 16%, transparent);
+  }
+
+  .hilal-panel-scroll {
+    padding-block-end: 18px;
+    scrollbar-width: none;
+  }
+
+  .hilal-panel-scroll::-webkit-scrollbar {
+    display: none;
+  }
+
+  .hilal-section {
+    gap: 8px;
+    margin-block-end: 20px;
+  }
+
+  .hilal-section-heading {
+    color: var(--hilal-sidebar-muted);
+    font-size: 13px;
+    font-weight: 700;
+    min-block-size: 26px;
+    padding-inline: 0 8px;
+    text-transform: none;
+  }
+
+  .hilal-row,
+  .hilal-footer-row {
+    border-radius: 10px;
+  }
+
+  .hilal-row {
+    min-block-size: 28px;
+    padding-inline: 8px;
+  }
+
+  .hilal-footer-row {
+    min-block-size: 30px;
+    padding-inline: 9px;
+  }
+
+  .hilal-count-badge {
+    background: light-dark(rgba(255, 255, 255, 0.44), rgba(255, 255, 255, 0.08));
+    border: 1px solid color-mix(in srgb, var(--hilal-sidebar-text) 8%, transparent);
+  }
+
+  .hilal-footer-group,
+  .hilal-sidebar-footer,
+  .hilal-workspace-add-wrap {
+    border-color: color-mix(in srgb, var(--hilal-sidebar-text) 10%, transparent);
+  }
+
+  #hilal-tabs-section {
+    min-height: unset !important;
+  }
+
+  .hilal-tabstrip-container {
+    gap: 2px;
+  }
+
+  /* Safari header layout */
+  .safari-sidebar-header {
+    display: flex;
+    align-items: flex-start;
+    justify-content: flex-end;
+    padding: 18px 18px 0 88px;
+    height: 58px;
+    min-height: 58px;
+    box-sizing: border-box;
+  }
+
+  .safari-header-buttons {
+    display: flex;
+    align-items: center;
+    gap: 12px;
+  }
+
+  .safari-icon-button {
+    background: transparent;
+    border: none;
+    color: var(--hilal-sidebar-muted);
+    cursor: pointer;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    width: 34px;
+    height: 34px;
+    border-radius: 9px;
+    padding: 0;
+    transition:
+      background-color 0.15s ease,
+      color 0.15s ease;
+  }
+
+  .safari-icon-button:hover {
+    background-color: var(--hilal-sidebar-hover);
+    color: var(--sidebar-text-color);
+  }
+
+  .safari-icon-button svg {
+    width: 18px;
+    height: 18px;
+  }
+
+  /* Layout and scroll panel */
+  .safari-sidebar-layout {
+    display: flex;
+    flex-direction: column;
+    height: 100%;
+    box-sizing: border-box;
+    padding-block-start: 8px !important;
+  }
+
+  .safari-panel {
+    flex: 1 1 auto;
+    display: flex;
+    flex-direction: column;
+    min-height: 0;
+    padding: 0 16px 0 16px !important;
+  }
+
+  .safari-scroll {
+    flex: 1 1 auto;
+    overflow-y: auto;
+    scrollbar-width: none;
+    padding-block-end: 24px;
+  }
+
+  .safari-scroll::-webkit-scrollbar {
+    display: none;
+  }
+
+  /* Section heading buttons (collapsible Tabs section) */
+  .safari-section-heading-btn {
+    background: transparent;
+    border: none;
+    color: var(--hilal-sidebar-muted);
+    font: inherit;
+    font-size: 13px;
+    font-weight: 700;
+    display: flex;
+    align-items: center;
+    gap: 8px;
+    padding: 6px 0;
+    width: 100%;
+    cursor: pointer;
+    text-align: start;
+    user-select: none;
+  }
+
+  .safari-section-heading-btn:hover {
+    color: var(--sidebar-text-color);
+  }
+
+  .safari-chevron {
+    transition: transform 0.2s ease;
+    transform: rotate(0deg);
+    opacity: 0.7;
+    margin-inline-start: 2px;
+  }
+
+  .safari-chevron-collapsed {
+    transform: rotate(-90deg);
+  }
+
+  .safari-heading-icon {
+    width: 16px;
+    height: 16px;
+    opacity: 0.85;
+  }
+
+  .safari-heading-text {
+    flex: 1 1 auto;
+  }
+
+  /* Section headers like Tab Groups / Saved */
+  .safari-section {
+    margin-block-end: 20px;
+    display: flex;
+    flex-direction: column;
+    gap: 4px;
+  }
+
+  .safari-section-heading {
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    height: 28px;
+    padding-inline: 4px;
+    color: var(--hilal-sidebar-muted);
+    font-size: 11px;
+    font-weight: 700;
+    text-transform: uppercase;
+    letter-spacing: 0;
+    user-select: none;
+  }
+
+  .safari-heading-title {
+    flex: 1 1 auto;
+  }
+
+  .safari-section-add-btn {
+    background: transparent;
+    border: none;
+    color: inherit;
+    cursor: pointer;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    width: 20px;
+    height: 20px;
+    border-radius: 4px;
+    padding: 0;
+    opacity: 0;
+    transition:
+      opacity 0.15s ease,
+      background-color 0.15s ease;
+  }
+
+  .safari-section:hover .safari-section-add-btn {
+    opacity: 1;
+  }
+
+  .safari-section-add-btn:hover {
+    background-color: var(--hilal-sidebar-hover);
+    color: var(--sidebar-text-color);
+  }
+
+  /* Safari Rows List & Items */
+  .safari-row-list {
+    display: flex;
+    flex-direction: column;
+    gap: 2px;
+  }
+
+  .safari-row {
+    background: transparent;
+    border: 1px solid transparent;
+    color: var(--sidebar-text-color);
+    font: inherit;
+    font-size: 13px;
+    font-weight: 500;
+    display: flex;
+    align-items: center;
+    gap: 10px;
+    width: 100%;
+    min-height: 30px;
+    height: 30px;
+    padding-inline: 7px;
+    border-radius: 8px;
+    cursor: pointer;
+    text-align: start;
+    transition:
+      background-color 0.15s ease,
+      color 0.15s ease,
+      opacity 0.15s ease,
+      border-color 0.15s ease,
+      box-shadow 0.15s ease,
+      transform 0.3s cubic-bezier(0.25, 1, 0.5, 1);
+    user-select: none;
+    box-sizing: border-box;
+  }
+
+  .safari-row:hover {
+    background-color: var(--hilal-sidebar-hover);
+  }
+
+  .safari-row-active {
+    background-color: var(--hilal-sidebar-highlight) !important;
+    font-weight: 600;
+  }
+
+  .safari-row.hilal-ws-drop-target {
+    background-color: var(--hilal-sidebar-highlight) !important;
+    border-color: light-dark(rgba(0, 0, 0, 0.15), rgba(255, 255, 255, 0.25)) !important;
+    box-shadow: 0 0 0 1.5px var(--hilal-sidebar-blue) !important;
+  }
+
+  .safari-tab-row.dragging-active {
+    position: relative;
+    z-index: 10000 !important;
+    background-color: light-dark(rgba(255, 255, 255, 0.42), rgba(255, 255, 255, 0.15)) !important;
+    backdrop-filter: blur(24px) !important;
+    -webkit-backdrop-filter: blur(24px) !important;
+    border-color: light-dark(rgba(0, 0, 0, 0.12), rgba(255, 255, 255, 0.22)) !important;
+    box-shadow: 0 10px 30px light-dark(rgba(0, 0, 0, 0.15), rgba(0, 0, 0, 0.45)),
+                0 1px 0 light-dark(rgba(255, 255, 255, 0.4), rgba(255, 255, 255, 0.1)) inset !important;
+    transition: none !important;
+    pointer-events: none;
+  }
+
+  .safari-tab-row.dropping-active {
+    position: relative;
+    z-index: 10000 !important;
+    background-color: light-dark(rgba(255, 255, 255, 0.42), rgba(255, 255, 255, 0.15)) !important;
+    backdrop-filter: blur(24px) !important;
+    -webkit-backdrop-filter: blur(24px) !important;
+    border-color: light-dark(rgba(0, 0, 0, 0.12), rgba(255, 255, 255, 0.22)) !important;
+    box-shadow: 0 4px 12px light-dark(rgba(0, 0, 0, 0.08), rgba(0, 0, 0, 0.2)),
+                0 1px 0 light-dark(rgba(255, 255, 255, 0.4), rgba(255, 255, 255, 0.1)) inset !important;
+    transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1), 
+                box-shadow 0.4s ease, 
+                background-color 0.4s ease, 
+                border-color 0.4s ease, 
+                backdrop-filter 0.4s ease !important;
+  }
+
+  @media (prefers-reduced-motion: reduce) {
+    .safari-row,
+    .safari-tab-row[dragging],
+    .safari-tab-row[dragover] {
+      transition: none !important;
+      transform: none !important;
+    }
+  }
+
+  .safari-row-emoji {
+    font-size: 14px;
+    line-height: 1;
+    display: inline-block;
+    width: 16px;
+    text-align: center;
+  }
+
+  .safari-row-icon {
+    width: 16px;
+    height: 16px;
+    color: var(--hilal-sidebar-muted);
+  }
+
+  .safari-row-active .safari-row-icon {
+    color: var(--sidebar-text-color);
+  }
+
+  .safari-row-title {
+    flex: 1 1 auto;
+    overflow: hidden;
+    text-overflow: ellipsis;
+    white-space: nowrap;
+  }
+
+  .safari-tab-row .safari-row-close-btn {
+    display: none;
+    background: transparent;
+    border: none;
+    padding: 4px;
+    border-radius: 4px;
+    color: var(--hilal-sidebar-muted);
+    cursor: pointer;
+    align-items: center;
+    justify-content: center;
+    width: 20px;
+    height: 20px;
+    margin-inline-start: 4px;
+  }
+
+  .safari-tab-row:hover .safari-row-close-btn {
+    display: flex;
+  }
+
+  .safari-tab-row .safari-row-close-btn:hover {
+    background-color: var(--hilal-sidebar-hover);
+    color: var(--sidebar-text-color);
+  }
+
+  .safari-row-audio {
+    cursor: pointer;
+    opacity: 0.7;
+    margin-inline-end: 4px;
+    display: flex;
+    align-items: center;
+  }
+
+  .safari-row-audio:hover {
+    opacity: 1;
+  }
+
+  .safari-row-pin {
+    color: var(--hilal-sidebar-muted);
+    margin-inline-end: 4px;
+    display: flex;
+    align-items: center;
+  }
+
+  .safari-bookmarks-sublist {
+    padding-inline-start: 16px;
+    display: flex;
+    flex-direction: column;
+    gap: 2px;
+    margin-block: 2px;
+  }
+
+  .safari-bookmarks-sublist .hilal-row {
+    font-size: 12.5px;
+    height: 28px;
+    min-height: 28px;
+    padding-inline: 8px;
+    border-radius: 6px;
+  }
+}
+
 .wrapper {
   display: grid;
   grid-template-rows: auto min-content min-content;
diff --git a/browser/components/sidebar/sidebar-main.mjs b/browser/components/sidebar/sidebar-main.mjs
--- a/browser/components/sidebar/sidebar-main.mjs
+++ b/browser/components/sidebar/sidebar-main.mjs
@@ -55,8 +55,14 @@ export default class SidebarMain extends MozLitElement {
     hilalFooterPrivacy: { type: Boolean },
     hilalFooterSettings: { type: Boolean },
     hilalCustomItems: { type: Array },
+    isTabsExpanded: { type: Boolean },
+    showHorizontal: { type: Boolean },
   };
 
+  get isTahoeEnabled() {
+    return Services.prefs.getBoolPref("hilal.tahoe.enabled", true);
+  }
+
   static queries = {
     allButtons: { all: "moz-button" },
     extensionButtons: { all: ".tools-and-extensions > moz-button[extension]" },
@@ -108,8 +114,13 @@ export default class SidebarMain extends MozLitElement {
     this._hilalPrefObserver = (_subject, topic, data) => {
       this.observe(_subject, topic, data);
     };
+    this._onTabPointerMove = this.handleTabPointerMove.bind(this);
+    this._onTabPointerUp = this.handleTabPointerUp.bind(this);
+    this._onTabPointerCancel = this.handleTabPointerCancel.bind(this);
     this.shouldShowOverflowButton = false;
     this.overflowMenuOpen = false;
+    this.isTabsExpanded = true;
+    this.showHorizontal = false;
   }
 
   tooltips = {
@@ -176,6 +187,11 @@ export default class SidebarMain extends MozLitElement {
     window.addEventListener("SidebarItemRemoved", this);
 
     Services.prefs.addObserver("sidebar.verticalTabs", this._hilalPrefObserver);
+    Services.prefs.addObserver(
+      "sidebar.verticalTabs.showHorizontal",
+      this._hilalPrefObserver
+    );
+    Services.prefs.addObserver("hilal.tahoe.enabled", this._hilalPrefObserver);
     Services.prefs.addObserver("hilal.workspaces.", this._hilalPrefObserver);
     Services.prefs.addObserver(
       "hilal.sidebar.footer.",
@@ -210,6 +226,14 @@ export default class SidebarMain extends MozLitElement {
       "sidebar.verticalTabs",
       this._hilalPrefObserver
     );
+    Services.prefs.removeObserver(
+      "sidebar.verticalTabs.showHorizontal",
+      this._hilalPrefObserver
+    );
+    Services.prefs.removeObserver(
+      "hilal.tahoe.enabled",
+      this._hilalPrefObserver
+    );
     Services.prefs.removeObserver("hilal.workspaces.", this._hilalPrefObserver);
     Services.prefs.removeObserver(
       "hilal.sidebar.footer.",
@@ -246,8 +270,14 @@ export default class SidebarMain extends MozLitElement {
       return true;
     }
     const manager = window.gHilalWorkspaces;
-    const workspacesEnabled = Services.prefs.getBoolPref("hilal.workspaces.enabled", true);
-    const verticalTabsEnabled = Services.prefs.getBoolPref("sidebar.verticalTabs", false);
+    const workspacesEnabled = Services.prefs.getBoolPref(
+      "hilal.workspaces.enabled",
+      true
+    );
+    const verticalTabsEnabled = Services.prefs.getBoolPref(
+      "sidebar.verticalTabs",
+      false
+    );
     if (!manager || (!workspacesEnabled && !verticalTabsEnabled)) {
       return false;
     }
@@ -343,15 +373,22 @@ export default class SidebarMain extends MozLitElement {
 
   observe(_subject, topic, data) {
     if (topic === "nsPref:changed") {
-      if (data === "sidebar.verticalTabs") {
+      if (
+        data === "sidebar.verticalTabs" ||
+        data === "sidebar.verticalTabs.showHorizontal"
+      ) {
         const enabled = Services.prefs.getBoolPref(
           "sidebar.verticalTabs",
           false
         );
         this._sidebarMain?.toggleAttribute("vertical-tabs", enabled);
         this.toggleAttribute("vertical-tabs", enabled);
-        const workspacesEnabled = Services.prefs.getBoolPref("hilal.workspaces.enabled", true);
-        const shouldBeActive = window.gHilalWorkspaces && (workspacesEnabled || enabled);
+        const workspacesEnabled = Services.prefs.getBoolPref(
+          "hilal.workspaces.enabled",
+          true
+        );
+        const shouldBeActive =
+          window.gHilalWorkspaces && (workspacesEnabled || enabled);
         if (shouldBeActive !== this.hilalSidebarActive) {
           if (shouldBeActive) {
             this.maybeInitHilalSidebar();
@@ -359,12 +396,22 @@ export default class SidebarMain extends MozLitElement {
             this.uninitHilalSidebar();
           }
         } else {
-          this.requestUpdate();
+          this.refreshHilalSidebar();
         }
-      } else if (data === "hilal.workspaces.enabled") {
-        const workspacesEnabled = Services.prefs.getBoolPref("hilal.workspaces.enabled", true);
-        const verticalTabsEnabled = Services.prefs.getBoolPref("sidebar.verticalTabs", false);
-        const shouldBeActive = window.gHilalWorkspaces && (workspacesEnabled || verticalTabsEnabled);
+      } else if (
+        data === "hilal.workspaces.enabled" ||
+        data === "hilal.tahoe.enabled"
+      ) {
+        const workspacesEnabled = Services.prefs.getBoolPref(
+          "hilal.workspaces.enabled",
+          true
+        );
+        const verticalTabsEnabled = Services.prefs.getBoolPref(
+          "sidebar.verticalTabs",
+          false
+        );
+        const shouldBeActive =
+          window.gHilalWorkspaces && (workspacesEnabled || verticalTabsEnabled);
         if (shouldBeActive !== this.hilalSidebarActive) {
           if (shouldBeActive) {
             this.maybeInitHilalSidebar();
@@ -403,6 +450,10 @@ export default class SidebarMain extends MozLitElement {
     this.hilalWorkspaces = [...(manager._workspaces || [])];
     this.hilalActiveId = manager._activeId || this.hilalWorkspaces[0]?.id || "";
     this.hilalTabs = this.getHilalWorkspaceTabs(this.hilalActiveId);
+    this.showHorizontal = Services.prefs.getBoolPref(
+      "sidebar.verticalTabs.showHorizontal",
+      false
+    );
     this.hilalShowBookmarks = Services.prefs.getBoolPref(
       "hilal.workspaces.showBookmarks",
       false
@@ -530,38 +581,354 @@ export default class SidebarMain extends MozLitElement {
     tab.dispatchEvent(new CustomEvent("TabHoverEnd", { bubbles: true }));
   }
 
+  handleTabPointerDown(tab, event) {
+    if (event.button !== 0) {
+      return;
+    }
+    const target = event.target;
+    if (target.closest(".safari-row-close-btn") || target.closest(".safari-row-audio")) {
+      return;
+    }
+
+    this._dragTab = tab;
+    this._dragElement = event.currentTarget;
+    this._activePointerId = event.pointerId;
+    this._startX = event.clientX;
+    this._startY = event.clientY;
+    this._isDragging = false;
+    this._hoveredWorkspaceRow = null;
+    this._targetWorkspaceId = null;
+
+    this._dragElement.setPointerCapture(event.pointerId);
+    this._dragElement.addEventListener("pointermove", this._onTabPointerMove);
+    this._dragElement.addEventListener("pointerup", this._onTabPointerUp);
+    this._dragElement.addEventListener("pointercancel", this._onTabPointerCancel);
+
+    event.stopPropagation();
+  }
+
+  handleTabPointerMove(event) {
+    if (event.pointerId !== this._activePointerId) {
+      return;
+    }
+
+    const clientX = event.clientX;
+    const clientY = event.clientY;
+
+    if (!this._isDragging) {
+      const dx = clientX - this._startX;
+      const dy = clientY - this._startY;
+      if (Math.sqrt(dx * dx + dy * dy) > 4) {
+        this._isDragging = true;
+
+        const container = this.shadowRoot.querySelector(".safari-row-list");
+        if (!container) {
+          this.handleTabPointerCancel(event);
+          return;
+        }
+
+        this._containerRect = container.getBoundingClientRect();
+        this._tabElements = Array.from(container.querySelectorAll(".safari-tab-row"));
+        this._tabPositions = [];
+        this._tabHeights = [];
+        this._dragIndex = -1;
+
+        for (let i = 0; i < this._tabElements.length; i++) {
+          const el = this._tabElements[i];
+          const rect = el.getBoundingClientRect();
+          this._tabPositions.push({
+            left: rect.left - this._containerRect.left,
+            top: rect.top - this._containerRect.top,
+            height: rect.height,
+            width: rect.width,
+          });
+          this._tabHeights.push(rect.height);
+          if (el === this._dragElement) {
+            this._dragIndex = i;
+          }
+        }
+
+        if (this._dragIndex === -1) {
+          this.handleTabPointerCancel(event);
+          return;
+        }
+
+        const dragRect = this._dragElement.getBoundingClientRect();
+        this._dragOffset = {
+          x: this._startX - dragRect.left,
+          y: this._startY - dragRect.top,
+        };
+
+        this._dragElement.classList.add("dragging-active");
+        this._targetIndex = this._dragIndex;
+
+        try {
+          this._dragTab.dispatchEvent(new CustomEvent("TabHoverEnd", { bubbles: true }));
+        } catch (e) {}
+      }
+      return;
+    }
+
+    event.preventDefault();
+    event.stopPropagation();
+
+    const originalPos = this._tabPositions[this._dragIndex];
+    const tx = (clientX - this._dragOffset.x - this._containerRect.left) - originalPos.left;
+    const ty = (clientY - this._dragOffset.y - this._containerRect.top) - originalPos.top;
+
+    this._dragElement.style.transform = `translate3d(${tx}px, ${ty}px, 0) scale(0.97)`;
+
+    const elementUnderPointer = this.shadowRoot.elementFromPoint(clientX, clientY);
+    const workspaceRow = elementUnderPointer?.closest(".safari-row");
+    const isWorkspace = workspaceRow && !workspaceRow.classList.contains("safari-tab-row");
+
+    if (isWorkspace) {
+      const wsId = workspaceRow.dataset.workspaceId;
+      if (this._hoveredWorkspaceRow !== workspaceRow) {
+        this._hoveredWorkspaceRow?.classList.remove("hilal-ws-drop-target");
+        this._hoveredWorkspaceRow = workspaceRow;
+        workspaceRow.classList.add("hilal-ws-drop-target");
+      }
+      this._targetWorkspaceId = wsId;
+      this._targetIndex = -1;
+
+      for (let j = 0; j < this._tabElements.length; j++) {
+        if (j !== this._dragIndex) {
+          this._tabElements[j].style.transform = "";
+        }
+      }
+      return;
+    }
+
+    if (this._hoveredWorkspaceRow) {
+      this._hoveredWorkspaceRow.classList.remove("hilal-ws-drop-target");
+      this._hoveredWorkspaceRow = null;
+    }
+    this._targetWorkspaceId = null;
+
+    const h = originalPos.height;
+    const y_top = clientY - this._dragOffset.y - this._containerRect.top;
+    const y_center = y_top + h / 2;
+
+    let computedTarget = 0;
+    let minDiff = Infinity;
+
+    for (let k = 0; k < this._tabPositions.length; k++) {
+      let slotTop = this._tabPositions[k].top;
+      if (k > this._dragIndex) {
+        slotTop -= this._tabHeights[this._dragIndex];
+      }
+      const slotCenter = slotTop + this._tabHeights[this._dragIndex] / 2;
+      const diff = Math.abs(y_center - slotCenter);
+      if (diff < minDiff) {
+        minDiff = diff;
+        computedTarget = k;
+      }
+    }
+
+    this._targetIndex = computedTarget;
+
+    for (let j = 0; j < this._tabElements.length; j++) {
+      if (j === this._dragIndex) {
+        continue;
+      }
+      const isShifted = (this._targetIndex <= j && j < this._dragIndex) || 
+                        (this._dragIndex < j && j <= this._targetIndex);
+      if (isShifted) {
+        const shiftY = j < this._dragIndex ? this._tabHeights[this._dragIndex] : -this._tabHeights[this._dragIndex];
+        const shiftX = 8;
+        this._tabElements[j].style.transform = `translate3d(${shiftX}px, ${shiftY}px, 0)`;
+      } else {
+        this._tabElements[j].style.transform = "";
+      }
+    }
+  }
+
+  handleTabPointerUp(event) {
+    if (event.pointerId !== this._activePointerId) {
+      return;
+    }
+
+    const element = this._dragElement;
+    element.releasePointerCapture(event.pointerId);
+    element.removeEventListener("pointermove", this._onTabPointerMove);
+    element.removeEventListener("pointerup", this._onTabPointerUp);
+    element.removeEventListener("pointercancel", this._onTabPointerCancel);
+
+    this._activePointerId = null;
+
+    if (!this._isDragging) {
+      this.selectHilalTab(this._dragTab);
+      this._dragTab = null;
+      this._dragElement = null;
+      return;
+    }
+
+    const draggedTab = this._dragTab;
+    const dragIndex = this._dragIndex;
+    const targetIndex = this._targetIndex;
+    const targetWorkspaceId = this._targetWorkspaceId;
+
+    this._hoveredWorkspaceRow?.classList.remove("hilal-ws-drop-target");
+    this._hoveredWorkspaceRow = null;
+
+    element.classList.remove("dragging-active");
+
+    if (targetWorkspaceId) {
+      this.cleanupDragStyles();
+      this._hilalManager?._moveTabToWorkspace?.(draggedTab, targetWorkspaceId);
+      this.scheduleHilalRefresh();
+      return;
+    }
+
+    if (dragIndex === -1 || targetIndex === -1 || targetIndex === dragIndex) {
+      element.classList.add("dropping-active");
+      element.style.transform = "translate3d(0, 0, 0) scale(1)";
+      setTimeout(() => {
+        this.cleanupDragStyles();
+        this.scheduleHilalRefresh();
+      }, 400);
+      return;
+    }
+
+    element.classList.add("dropping-active");
+    const finalTy = this._tabPositions[targetIndex].top - this._tabPositions[dragIndex].top;
+    element.style.transform = `translate3d(0px, ${finalTy}px, 0) scale(1)`;
+
+    setTimeout(() => {
+      const targetTab = this.filteredHilalTabs[targetIndex];
+      if (draggedTab && targetTab && draggedTab !== targetTab) {
+        if (draggedTab.pinned && !targetTab.pinned) {
+          window.gBrowser.unpinTab(draggedTab);
+        } else if (!draggedTab.pinned && targetTab.pinned) {
+          window.gBrowser.pinTab(draggedTab);
+        }
+        window.gBrowser.moveTabTo(draggedTab, targetTab._tPos);
+      }
+      this.cleanupDragStyles();
+      this.scheduleHilalRefresh();
+    }, 400);
+  }
+
+  handleTabPointerCancel(event) {
+    if (event.pointerId !== this._activePointerId) {
+      return;
+    }
+
+    const element = this._dragElement;
+    if (element) {
+      element.releasePointerCapture(event.pointerId);
+      element.removeEventListener("pointermove", this._onTabPointerMove);
+      element.removeEventListener("pointerup", this._onTabPointerUp);
+      element.removeEventListener("pointercancel", this._onTabPointerCancel);
+    }
+
+    this._activePointerId = null;
+    this._hoveredWorkspaceRow?.classList.remove("hilal-ws-drop-target");
+    this._hoveredWorkspaceRow = null;
+
+    if (this._isDragging) {
+      element?.classList.remove("dragging-active");
+      element?.classList.add("dropping-active");
+      if (element) {
+        element.style.transform = "translate3d(0, 0, 0) scale(1)";
+      }
+      setTimeout(() => {
+        this.cleanupDragStyles();
+        this.scheduleHilalRefresh();
+      }, 400);
+    } else {
+      this._dragTab = null;
+      this._dragElement = null;
+    }
+  }
+
+  cleanupDragStyles() {
+    if (this._dragElement) {
+      this._dragElement.classList.remove("dragging-active");
+      this._dragElement.classList.remove("dropping-active");
+      this._dragElement.style.transform = "";
+    }
+    if (this._tabElements) {
+      for (const el of this._tabElements) {
+        el.style.transform = "";
+      }
+    }
+    this._dragTab = null;
+    this._dragElement = null;
+    this._tabElements = [];
+    this._tabPositions = [];
+    this._tabHeights = [];
+    this._isDragging = false;
+    this._dragIndex = -1;
+    this._targetIndex = -1;
+    this._targetWorkspaceId = null;
+  }
+
   handleTabDragStart(tab, event) {
+    this.clearHilalTabDragState();
     this._draggedTab = tab;
+    this._draggedTabRow = event.currentTarget;
+    event.currentTarget.toggleAttribute("dragging", true);
     event.dataTransfer.effectAllowed = "move";
     event.dataTransfer.setData("text/plain", tab.label);
+    event.dataTransfer.setData("application/x-hilal-tab", String(tab._tPos));
+    event.dataTransfer.setDragImage(
+      event.currentTarget,
+      18,
+      Math.round(event.currentTarget.getBoundingClientRect().height / 2)
+    );
     try {
       tab.dispatchEvent(new CustomEvent("TabHoverEnd", { bubbles: true }));
     } catch (e) {}
   }
 
+  handleTabDragEnd(event) {
+    this.clearHilalTabDragState();
+  }
+
+  clearHilalTabDragState() {
+    this._draggedTabRow?.removeAttribute("dragging");
+    this._dragOverTabRow?.removeAttribute("dragover");
+    this._draggedTab = null;
+    this._draggedTabRow = null;
+    this._dragOverTabRow = null;
+  }
+
+  setHilalTabDropTarget(row) {
+    if (this._dragOverTabRow === row) {
+      return;
+    }
+    this._dragOverTabRow?.removeAttribute("dragover");
+    this._dragOverTabRow = row;
+    row?.toggleAttribute("dragover", true);
+  }
+
   handleTabDragOver(tab, event) {
     if (!this._draggedTab) {
       return;
     }
     event.preventDefault();
     event.dataTransfer.dropEffect = "move";
+    this.setHilalTabDropTarget(event.currentTarget);
   }
 
   handleTabDrop(targetTab, event) {
     event.preventDefault();
     event.stopPropagation();
-    if (!this._draggedTab || this._draggedTab === targetTab) {
+    const draggedTab = this._draggedTab;
+    this.clearHilalTabDragState();
+    if (!draggedTab || draggedTab === targetTab) {
       return;
     }
 
-    if (this._draggedTab.pinned && !targetTab.pinned) {
-      window.gBrowser.unpinTab(this._draggedTab);
-    } else if (!this._draggedTab.pinned && targetTab.pinned) {
-      window.gBrowser.pinTab(this._draggedTab);
+    if (draggedTab.pinned && !targetTab.pinned) {
+      window.gBrowser.unpinTab(draggedTab);
+    } else if (!draggedTab.pinned && targetTab.pinned) {
+      window.gBrowser.pinTab(draggedTab);
     }
 
-    window.gBrowser.moveTabTo(this._draggedTab, targetTab._tPos);
-    this._draggedTab = null;
+    window.gBrowser.moveTabTo(draggedTab, targetTab._tPos);
     this.scheduleHilalRefresh();
   }
 
@@ -575,15 +942,16 @@ export default class SidebarMain extends MozLitElement {
 
   handlePinnedContainerDrop(event) {
     event.preventDefault();
-    if (!this._draggedTab) {
+    const draggedTab = this._draggedTab;
+    this.clearHilalTabDragState();
+    if (!draggedTab) {
       return;
     }
 
-    if (!this._draggedTab.pinned) {
-      window.gBrowser.pinTab(this._draggedTab);
+    if (!draggedTab.pinned) {
+      window.gBrowser.pinTab(draggedTab);
     }
 
-    this._draggedTab = null;
     this.scheduleHilalRefresh();
   }
 
@@ -597,21 +965,22 @@ export default class SidebarMain extends MozLitElement {
 
   handleUnpinnedContainerDrop(event) {
     event.preventDefault();
-    if (!this._draggedTab) {
+    const draggedTab = this._draggedTab;
+    this.clearHilalTabDragState();
+    if (!draggedTab) {
       return;
     }
 
-    if (this._draggedTab.pinned) {
-      window.gBrowser.unpinTab(this._draggedTab);
+    if (draggedTab.pinned) {
+      window.gBrowser.unpinTab(draggedTab);
     }
 
     const workspaceTabs = this.filteredHilalUnpinnedTabs;
     if (workspaceTabs.length > 0) {
       const lastTab = workspaceTabs[workspaceTabs.length - 1];
-      window.gBrowser.moveTabTo(this._draggedTab, lastTab._tPos);
+      window.gBrowser.moveTabTo(draggedTab, lastTab._tPos);
     }
 
-    this._draggedTab = null;
     this.scheduleHilalRefresh();
   }
 
@@ -705,7 +1074,11 @@ export default class SidebarMain extends MozLitElement {
 
   handleWorkspaceDragOver(workspace, event) {
     const types = event.dataTransfer.types;
-    if (this._draggedTab || types.includes("application/x-moz-tabbrowser-tab") || types.includes("application/x-hilal-workspace")) {
+    if (
+      this._draggedTab ||
+      types.includes("application/x-moz-tabbrowser-tab") ||
+      types.includes("application/x-hilal-workspace")
+    ) {
       event.preventDefault();
       event.dataTransfer.dropEffect = "move";
       event.currentTarget.classList.add("hilal-ws-drop-target");
@@ -720,22 +1093,28 @@ export default class SidebarMain extends MozLitElement {
     event.currentTarget.classList.remove("hilal-ws-drop-target");
 
     if (this._draggedTab) {
+      const draggedTab = this._draggedTab;
       event.preventDefault();
-      this._hilalManager?._moveTabToWorkspace?.(this._draggedTab, workspace.id);
-      this._draggedTab = null;
+      this.clearHilalTabDragState();
+      this._hilalManager?._moveTabToWorkspace?.(draggedTab, workspace.id);
       this.scheduleHilalRefresh();
       return;
     }
 
     const types = event.dataTransfer.types;
     if (types.includes("application/x-moz-tabbrowser-tab")) {
-      const draggedTab = event.dataTransfer.mozGetDataAt("application/x-moz-tabbrowser-tab", 0);
+      const draggedTab = event.dataTransfer.mozGetDataAt(
+        "application/x-moz-tabbrowser-tab",
+        0
+      );
       if (draggedTab && window.gBrowser.isTab(draggedTab)) {
         event.preventDefault();
         this._hilalManager?._moveTabToWorkspace?.(draggedTab, workspace.id);
       }
     } else if (types.includes("application/x-hilal-workspace")) {
-      const draggedWsId = event.dataTransfer.getData("application/x-hilal-workspace");
+      const draggedWsId = event.dataTransfer.getData(
+        "application/x-hilal-workspace"
+      );
       if (draggedWsId && draggedWsId !== workspace.id) {
         event.preventDefault();
         this._hilalManager?.reorderWorkspaces?.(draggedWsId, workspace.id);
@@ -789,8 +1168,44 @@ export default class SidebarMain extends MozLitElement {
     }
   }
 
+  toggleCompactMode() {
+    const prefName = "hilal.compact.enabled";
+    const current = Services.prefs.getBoolPref(prefName, false);
+    Services.prefs.setBoolPref(prefName, !current);
+  }
+
+  toggleSafariSidebar() {
+    const controller = window.SidebarController;
+    if (
+      !controller ||
+      !controller._state ||
+      controller.inSingleTabWindow ||
+      controller.uninitializing
+    ) {
+      return;
+    }
+
+    const shouldShow = !controller.launcherVisible;
+    if (
+      Services.prefs.getStringPref("sidebar.visibility", "always-show") !==
+      "hide-sidebar"
+    ) {
+      Services.prefs.setStringPref("sidebar.visibility", "hide-sidebar");
+      controller._state.revampVisibility = "hide-sidebar";
+    }
+    if (controller._animationEnabled && !window.gReduceMotion) {
+      controller._animateSidebarMain();
+    }
+    controller._state.updateVisibility(shouldShow, shouldShow);
+    if (!shouldShow) {
+      controller.hide({ dismissPanel: false });
+    }
+    controller.updateToolbarButton();
+  }
+
   openHilalFooterItem(item) {
-    const isCompact = document.documentElement.hasAttribute("hilal-compact-mode");
+    const isCompact =
+      document.documentElement.hasAttribute("hilal-compact-mode");
     if (typeof item === "object" && item !== null) {
       const url = this.normalizeHilalShortcutUrl(item.url);
       if (url === "viewBookmarksSidebar" || url === "about:bookmarks") {
@@ -813,13 +1228,18 @@ export default class SidebarMain extends MozLitElement {
         if (isCompact) {
           window.openTrustedLinkIn("about:logins", "tab", {
             userContextId: 0,
-            triggeringPrincipal: Services.scriptSecurityManager.getSystemPrincipal(),
+            triggeringPrincipal:
+              Services.scriptSecurityManager.getSystemPrincipal(),
           });
         } else {
           window.SidebarController.show("viewCPMSidebar");
         }
       } else if (url === "about:downloads") {
-        if (!isCompact && window.DownloadsPanel && window.DownloadsButton?.getAnchor()) {
+        if (
+          !isCompact &&
+          window.DownloadsPanel &&
+          window.DownloadsButton?.getAnchor()
+        ) {
           window.DownloadsPanel.showPanel(true);
         } else {
           window.BrowserCommands.downloadsUI();
@@ -866,14 +1286,19 @@ export default class SidebarMain extends MozLitElement {
         if (isCompact) {
           window.openTrustedLinkIn("about:logins", "tab", {
             userContextId: 0,
-            triggeringPrincipal: Services.scriptSecurityManager.getSystemPrincipal(),
+            triggeringPrincipal:
+              Services.scriptSecurityManager.getSystemPrincipal(),
           });
         } else {
           window.SidebarController.show("viewCPMSidebar");
         }
         break;
       case "downloads":
-        if (!isCompact && window.DownloadsPanel && window.DownloadsButton?.getAnchor()) {
+        if (
+          !isCompact &&
+          window.DownloadsPanel &&
+          window.DownloadsButton?.getAnchor()
+        ) {
           window.DownloadsPanel.showPanel(true);
         } else {
           window.BrowserCommands.downloadsUI();
@@ -1657,6 +2082,7 @@ export default class SidebarMain extends MozLitElement {
           "hilal-workspace-button": true,
           "hilal-workspace-active": active,
         })}
+        data-workspace-id=${workspace.id}
         type="button"
         title=${tooltip}
         aria-label=${ariaLabel}
@@ -1677,6 +2103,155 @@ export default class SidebarMain extends MozLitElement {
     `;
   }
 
+  hilalCustomTabTemplate(tab) {
+    const active = tab.selected;
+    const title = this.getHilalTabTitle(tab);
+    const icon = this.getHilalTabIcon(tab);
+
+    return html`
+      <div
+        class=${classMap({
+          "safari-row": true,
+          "safari-row-active": active,
+          "safari-tab-row": true,
+          "safari-pinned-tab": tab.pinned,
+        })}
+        role="button"
+        tabindex="0"
+        draggable="false"
+        title=${title}
+        @click=${event => {
+          if (this._isDragging) {
+            event.preventDefault();
+            event.stopPropagation();
+          }
+        }}
+        @keydown=${event => {
+          if (event.key === " " || event.key === "Enter") {
+            event.preventDefault();
+            this.selectHilalTab(tab);
+          }
+        }}
+        @mouseenter=${event => this.handleTabMouseEnter(tab, event)}
+        @mouseleave=${event => this.handleTabMouseLeave(tab, event)}
+        @pointerdown=${event => this.handleTabPointerDown(tab, event)}
+        @contextmenu=${event => {
+          event.preventDefault();
+          event.stopPropagation();
+          const contextMenu = document.getElementById("tabContextMenu");
+          if (contextMenu) {
+            event.currentTarget.tab = tab;
+            contextMenu.openPopup(
+              event.currentTarget,
+              "after_start",
+              0,
+              0,
+              true,
+              false,
+              event
+            );
+          }
+        }}
+      >
+        <!-- Favicon -->
+        <img class="safari-row-icon" src=${icon} width="16" height="16" />
+
+        <span class="safari-row-title">${title}</span>
+
+        <!-- Audio Indicator -->
+        ${when(
+          tab.soundPlaying || tab.muted,
+          () => html`
+            <span
+              class="safari-row-audio"
+              @click=${e => {
+                e.stopPropagation();
+                tab.toggleMuteAudio();
+                this.scheduleHilalRefresh();
+              }}
+            >
+              ${tab.muted
+                ? html`
+                    <svg
+                      width="12"
+                      height="12"
+                      viewBox="0 0 16 16"
+                      fill="none"
+                      stroke="currentColor"
+                      stroke-width="1.5"
+                      stroke-linecap="round"
+                      stroke-linejoin="round"
+                    >
+                      <path d="M11 5L5 9H2V13H5L11 17V5Z" />
+                      <line x1="23" y1="9" x2="17" y2="15" />
+                      <line x1="17" y1="9" x2="23" y2="15" />
+                    </svg>
+                  `
+                : html`
+                    <svg
+                      width="12"
+                      height="12"
+                      viewBox="0 0 16 16"
+                      fill="none"
+                      stroke="currentColor"
+                      stroke-width="1.5"
+                      stroke-linecap="round"
+                      stroke-linejoin="round"
+                    >
+                      <path d="M11 5L5 9H2V13H5L11 17V5Z" />
+                      <path d="M15.54 8.46a5 5 0 0 1 0 7.07" />
+                      <path d="M19.07 4.93a10 10 0 0 1 0 14.14" />
+                    </svg>
+                  `}
+            </span>
+          `
+        )}
+
+        <!-- Pin indicator/badge if pinned -->
+        ${when(
+          tab.pinned,
+          () => html`
+            <span class="safari-row-pin">
+              <svg
+                width="10"
+                height="10"
+                viewBox="0 0 16 16"
+                fill="currentColor"
+              >
+                <path
+                  d="M9.828.722a.5.5 0 0 1 .354.146l4.95 4.95a.5.5 0 0 1 0 .707c-.18.18-.362.393-.528.6a2.5 2.5 0 0 1-1.89 1.137l-.822.082a1.5 1.5 0 0 0-1.322 1.17l-.17.679a2.5 2.5 0 0 1-.94 1.348c-.282.193-.564.33-.746.402a.5.5 0 0 1-.63-.226l-2.483-4.966a.5.5 0 0 1 .082-.572L7.1 5.304a1.5 1.5 0 0 0 .424-1.128l-.066-.66a2.5 2.5 0 0 1 .59-1.808c.24-.265.485-.47.66-.618a.5.5 0 0 1 .35-.126zM2 13.5a.5.5 0 0 1 .82-.38l2.155 1.83a.5.5 0 1 1-.65.76l-2.155-1.83A.5.5 0 0 1 2 13.5z"
+                />
+              </svg>
+            </span>
+          `
+        )}
+
+        <!-- Close Tab Button -->
+        <button
+          class="safari-row-close-btn"
+          type="button"
+          title="Close Tab"
+          @click=${e => {
+            this.closeHilalTab(tab, e);
+          }}
+        >
+          <svg
+            width="8"
+            height="8"
+            viewBox="0 0 16 16"
+            fill="none"
+            stroke="currentColor"
+            stroke-width="2"
+            stroke-linecap="round"
+            stroke-linejoin="round"
+          >
+            <line x1="3" y1="3" x2="13" y2="13" />
+            <line x1="13" y1="3" x2="3" y2="13" />
+          </svg>
+        </button>
+      </div>
+    `;
+  }
 
   hilalBookmarkTemplate(bookmark) {
     const badge = this.getHilalBookmarkBadge(bookmark);
@@ -1860,14 +2435,23 @@ export default class SidebarMain extends MozLitElement {
                   `
                 )}
                 <span class="hilal-panel-title">
-                  ${(Services.prefs.getBoolPref("hilal.workspaces.enabled", true) && activeWorkspace) ? activeWorkspace.name : workspaceFallback}
+                  ${Services.prefs.getBoolPref(
+                    "hilal.workspaces.enabled",
+                    true
+                  ) && activeWorkspace
+                    ? activeWorkspace.name
+                    : workspaceFallback}
                 </span>
                 <span class="hilal-row-spacer"></span>
                 <button
                   class="hilal-icon-button"
                   type="button"
-                  title=${this.fluentStrings.formatValueSync("hilal-sidebar-new-tab-label")}
-                  aria-label=${this.fluentStrings.formatValueSync("hilal-sidebar-new-tab-label")}
+                  title=${this.fluentStrings.formatValueSync(
+                    "hilal-sidebar-new-tab-label"
+                  )}
+                  aria-label=${this.fluentStrings.formatValueSync(
+                    "hilal-sidebar-new-tab-label"
+                  )}
                   @click=${() => this.openHilalNewTab()}
                 >
                   ${this.hilalIcon("plus")}
@@ -1888,7 +2472,8 @@ export default class SidebarMain extends MozLitElement {
                       class="hilal-icon-button"
                       type="button"
                       data-l10n-id="hilal-sidebar-workspace-menu"
-                      @click=${event => this.showHilalActiveWorkspaceMenu(event)}
+                      @click=${event =>
+                        this.showHilalActiveWorkspaceMenu(event)}
                     >
                       ${this.hilalIcon("menu")}
                     </button>
@@ -1922,7 +2507,20 @@ export default class SidebarMain extends MozLitElement {
                     >
                   </div>
                   <div class="hilal-tabstrip-container">
-                    <slot name="tabstrip"></slot>
+                    ${this.showHorizontal
+                      ? html`
+                          <div class="safari-row-list">
+                            ${repeat(
+                              this.filteredHilalTabs,
+                              tab =>
+                                tab.linkedBrowser?.outerWindowID ||
+                                tab.id ||
+                                Math.random(),
+                              tab => this.hilalCustomTabTemplate(tab)
+                            )}
+                          </div>
+                        `
+                      : html`<slot name="tabstrip"></slot>`}
                   </div>
                 </section>
                 ${when(
@@ -2018,8 +2616,309 @@ export default class SidebarMain extends MozLitElement {
     `;
   }
 
+  tahoeSidebarTemplate() {
+    const activeWorkspace = this.activeHilalWorkspace;
+    const workspaceFallback = this.fluentStrings.formatValueSync(
+      "hilal-sidebar-workspace-fallback"
+    );
+    const railLabel = this.fluentStrings.formatValueSync(
+      "hilal-sidebar-workspaces-nav-label"
+    );
+    const panelLabel = this.fluentStrings.formatValueSync(
+      "hilal-sidebar-panel-label"
+    );
+
+    return html`
+      <link
+        rel="stylesheet"
+        href="chrome://browser/content/sidebar/sidebar.css"
+      />
+      <link
+        rel="stylesheet"
+        href="chrome://browser/content/sidebar/sidebar-main.css"
+      />
+      <div class="wrapper" hidden></div>
+      <div class="hilal-sidebar-layout safari-sidebar-layout">
+        <!-- Top Header with buttons matching Safari -->
+        <header class="safari-sidebar-header">
+          <div class="safari-header-buttons">
+            <button
+              class="safari-icon-button safari-toggle-button"
+              type="button"
+              title="Toggle Sidebar"
+              @click=${() => this.toggleSafariSidebar()}
+            >
+              <svg
+                width="16"
+                height="16"
+                viewBox="0 0 16 16"
+                fill="none"
+                stroke="currentColor"
+                stroke-width="1.3"
+                stroke-linecap="round"
+                stroke-linejoin="round"
+              >
+                <rect x="2" y="2" width="12" height="12" rx="2" />
+                <line x1="6" y1="2" x2="6" y2="14" />
+              </svg>
+            </button>
+          </div>
+        </header>
+
+        ${when(
+          this.hilalSearchOpen,
+          () => html`
+            <input
+              id="hilal-sidebar-search"
+              type="search"
+              data-l10n-id="hilal-sidebar-search-input"
+              .value=${this.hilalSearchQuery}
+              @input=${event => {
+                this.hilalSearchQuery = event.target.value;
+              }}
+            />
+          `
+        )}
+
+        <section
+          class="hilal-sidebar-panel safari-panel"
+          aria-label=${panelLabel}
+        >
+          <div class="hilal-panel-scroll safari-scroll">
+            <!-- Open Tabs / N Tabs Section -->
+            <section
+              class="hilal-section safari-section"
+              id="hilal-tabs-section"
+            >
+              <button
+                class="safari-section-heading-btn"
+                type="button"
+                @click=${() => {
+                  this.isTabsExpanded = !this.isTabsExpanded;
+                }}
+              >
+                <!-- Arrow / chevron indicator -->
+                <svg
+                  class=${classMap({
+                    "safari-chevron": true,
+                    "safari-chevron-collapsed": !this.isTabsExpanded,
+                  })}
+                  width="10"
+                  height="10"
+                  viewBox="0 0 10 10"
+                  fill="none"
+                  stroke="currentColor"
+                  stroke-width="1.3"
+                  stroke-linecap="round"
+                  stroke-linejoin="round"
+                >
+                  <path d="M2 3l3 3 3-3" />
+                </svg>
+
+                <!-- Window/Tabs Icon -->
+                <svg
+                  class="safari-heading-icon"
+                  width="16"
+                  height="16"
+                  viewBox="0 0 16 16"
+                  fill="none"
+                  stroke="currentColor"
+                  stroke-width="1.3"
+                  stroke-linecap="round"
+                  stroke-linejoin="round"
+                >
+                  <rect x="2" y="3" width="12" height="10" rx="1.5" />
+                  <line x1="2" y1="6" x2="14" y2="6" />
+                </svg>
+
+                <span class="safari-heading-text"
+                  >${this.hilalTabs.length} Tabs</span
+                >
+              </button>
+
+              <div
+                class="hilal-tabstrip-container"
+                ?hidden=${!this.isTabsExpanded}
+              >
+                ${this.showHorizontal
+                  ? html`
+                      <div class="safari-row-list">
+                        ${repeat(
+                          this.filteredHilalTabs,
+                          tab =>
+                            tab.linkedBrowser?.outerWindowID ||
+                            tab.id ||
+                            Math.random(),
+                          tab => this.hilalCustomTabTemplate(tab)
+                        )}
+                      </div>
+                    `
+                  : html`<slot name="tabstrip"></slot>`}
+              </div>
+            </section>
+
+            <!-- Tab Groups (Workspaces) Section -->
+            <section class="hilal-section safari-section">
+              <div class="safari-section-heading">
+                <span class="safari-heading-title">Tab Groups</span>
+                <button
+                  class="safari-section-add-btn"
+                  type="button"
+                  title="New Tab Group"
+                  @click=${() => this.showHilalCreateWorkspace()}
+                >
+                  <svg
+                    width="12"
+                    height="12"
+                    viewBox="0 0 16 16"
+                    fill="none"
+                    stroke="currentColor"
+                    stroke-width="1.5"
+                    stroke-linecap="round"
+                    stroke-linejoin="round"
+                  >
+                    <line x1="8" y1="3" x2="8" y2="13" />
+                    <line x1="3" y1="8" x2="13" y2="8" />
+                  </svg>
+                </button>
+              </div>
+
+              <div class="safari-row-list">
+                ${repeat(
+                  this.hilalWorkspaces,
+                  workspace => workspace.id,
+                  workspace => {
+                    const active = workspace.id === this.hilalActiveId;
+                    return html`
+                      <button
+                        class=${classMap({
+                          "safari-row": true,
+                          "safari-row-active": active,
+                        })}
+                        data-workspace-id=${workspace.id}
+                        type="button"
+                        draggable="true"
+                        @click=${() => this.switchHilalWorkspace(workspace)}
+                        @contextmenu=${event =>
+                          this.showHilalWorkspaceMenu(workspace, event)}
+                        @dragstart=${event =>
+                          this.handleWorkspaceDragStart(workspace, event)}
+                        @dragend=${event => this.handleWorkspaceDragEnd(event)}
+                        @dragover=${event =>
+                          this.handleWorkspaceDragOver(workspace, event)}
+                        @dragleave=${event =>
+                          this.handleWorkspaceDragLeave(event)}
+                        @drop=${event =>
+                          this.handleWorkspaceDrop(workspace, event)}
+                      >
+                        <!-- Workspace Icon or Emoji -->
+                        ${workspace.emoji
+                          ? html`<span
+                              class="safari-row-emoji"
+                              aria-hidden="true"
+                              >${workspace.emoji}</span
+                            >`
+                          : html`
+                              <svg
+                                class="safari-row-icon"
+                                width="16"
+                                height="16"
+                                viewBox="0 0 16 16"
+                                fill="none"
+                                stroke="currentColor"
+                                stroke-width="1.3"
+                                stroke-linecap="round"
+                                stroke-linejoin="round"
+                              >
+                                <rect
+                                  x="2"
+                                  y="4"
+                                  width="8"
+                                  height="8"
+                                  rx="1.5"
+                                />
+                                <rect
+                                  x="5"
+                                  y="2"
+                                  width="8"
+                                  height="8"
+                                  rx="1.5"
+                                />
+                              </svg>
+                            `}
+                        <span class="safari-row-title">${workspace.name}</span>
+                      </button>
+                    `;
+                  }
+                )}
+              </div>
+            </section>
+
+            <!-- Saved Section -->
+            <section class="hilal-section safari-section">
+              <div class="safari-section-heading">
+                <span class="safari-heading-title">Saved</span>
+              </div>
+
+              <div class="safari-row-list">
+                <!-- Bookmarks Row -->
+                <button
+                  class=${classMap({
+                    "safari-row": true,
+                    "safari-row-active": this.hilalShowBookmarks,
+                  })}
+                  type="button"
+                  @click=${() => {
+                    this.hilalShowBookmarks = !this.hilalShowBookmarks;
+                    Services.prefs.setBoolPref(
+                      "hilal.workspaces.showBookmarks",
+                      this.hilalShowBookmarks
+                    );
+                  }}
+                >
+                  <svg
+                    class="safari-row-icon"
+                    width="16"
+                    height="16"
+                    viewBox="0 0 16 16"
+                    fill="none"
+                    stroke="currentColor"
+                    stroke-width="1.3"
+                    stroke-linecap="round"
+                    stroke-linejoin="round"
+                  >
+                    <path d="M2 3h4a2 2 0 0 1 2 2v9a2 2 0 0 0-2-2H2z" />
+                    <path d="M14 3h-4a2 2 0 0 0-2 2v9a2 2 0 0 1 2-2h4z" />
+                  </svg>
+                  <span class="safari-row-title">Bookmarks</span>
+                </button>
+
+                <!-- Bookmarks Sub-list if expanded -->
+                ${when(
+                  this.hilalShowBookmarks,
+                  () => html`
+                    <div class="safari-bookmarks-sublist">
+                      ${repeat(
+                        this.filteredHilalBookmarks,
+                        bookmark => bookmark.guid,
+                        bookmark => this.hilalBookmarkTemplate(bookmark)
+                      )}
+                    </div>
+                  `
+                )}
+              </div>
+            </section>
+          </div>
+        </section>
+      </div>
+    `;
+  }
+
   render() {
     if (this.hilalSidebarActive) {
+      if (this.isTahoeEnabled) {
+        return this.tahoeSidebarTemplate();
+      }
       return this.hilalSidebarTemplate();
     }
     /* Add 1 to tools and extensions count for "Customize sidebar" option */
diff --git a/browser/components/tabbrowser/content/drag-and-drop.js b/browser/components/tabbrowser/content/drag-and-drop.js
--- a/browser/components/tabbrowser/content/drag-and-drop.js
+++ b/browser/components/tabbrowser/content/drag-and-drop.js
@@ -1179,69 +1179,81 @@
         canvas.mozOpaque = true;
       }
 
-      canvas.width = 160 * scale;
-      canvas.height = 90 * scale;
-      let toDrag = canvas;
-      let dragImageOffset = -16;
-      let splitViewTab;
-      if (isSplitViewWrapper(tab)) {
-        splitViewTab = tab.tabs.find(t => t.selected);
-      }
-      let browser = splitViewTab
-        ? splitViewTab.linkedBrowser
-        : isTab(tab) && tab.linkedBrowser;
-      if (isTabGroupLabel(tab)) {
-        toDrag = tab;
-      } else if (gMultiProcessBrowser) {
-        var context = canvas.getContext("2d");
-        context.fillStyle = "white";
-        context.fillRect(0, 0, canvas.width, canvas.height);
-
-        let captureListener;
-        let platform = AppConstants.platform;
-        // On Windows and Mac we can update the drag image during a drag
-        // using updateDragImage. On Linux, we can use a panel.
-        if (platform == "win" || platform == "macosx") {
-          captureListener = function () {
-            dt.updateDragImage(canvas, dragImageOffset, dragImageOffset);
-          };
-        } else {
-          // Create a panel to use it in setDragImage
-          // which will tell xul to render a panel that follows
-          // the pointer while a dnd session is on.
-          if (!this._tabbrowserTabs._dndPanel) {
-            this._tabbrowserTabs._dndCanvas = canvas;
-            this._tabbrowserTabs._dndPanel = document.createXULElement("panel");
-            this._tabbrowserTabs._dndPanel.className = "dragfeedback-tab";
-            this._tabbrowserTabs._dndPanel.setAttribute("type", "drag");
-            let wrapper = document.createElementNS(
-              "http://www.w3.org/1999/xhtml",
-              "div"
-            );
-            wrapper.style.width = "160px";
-            wrapper.style.height = "90px";
-            wrapper.appendChild(canvas);
-            this._tabbrowserTabs._dndPanel.appendChild(wrapper);
-            document.documentElement.appendChild(
-              this._tabbrowserTabs._dndPanel
-            );
+      let useTabDragImage = false;
+      try {
+        useTabDragImage = Services.prefs.getBoolPref("hilal.tahoe.enabled", true);
+      } catch (e) {}
+
+      if (useTabDragImage && !isTabGroupLabel(tab)) {
+        let rect = tab.getBoundingClientRect();
+        let xOffset = event.clientX - rect.left;
+        let yOffset = event.clientY - rect.top;
+        dt.setDragImage(tab, xOffset, yOffset);
+      } else {
+        canvas.width = 160 * scale;
+        canvas.height = 90 * scale;
+        let toDrag = canvas;
+        let dragImageOffset = -16;
+        let splitViewTab;
+        if (isSplitViewWrapper(tab)) {
+          splitViewTab = tab.tabs.find(t => t.selected);
+        }
+        let browser = splitViewTab
+          ? splitViewTab.linkedBrowser
+          : isTab(tab) && tab.linkedBrowser;
+        if (isTabGroupLabel(tab)) {
+          toDrag = tab;
+        } else if (gMultiProcessBrowser) {
+          var context = canvas.getContext("2d");
+          context.fillStyle = "white";
+          context.fillRect(0, 0, canvas.width, canvas.height);
+
+          let captureListener;
+          let platform = AppConstants.platform;
+          // On Windows and Mac we can update the drag image during a drag
+          // using updateDragImage. On Linux, we can use a panel.
+          if (platform == "win" || platform == "macosx") {
+            captureListener = function () {
+              dt.updateDragImage(canvas, dragImageOffset, dragImageOffset);
+            };
+          } else {
+            // Create a panel to use it in setDragImage
+            // which will tell xul to render a panel that follows
+            // the pointer while a dnd session is on.
+            if (!this._tabbrowserTabs._dndPanel) {
+              this._tabbrowserTabs._dndCanvas = canvas;
+              this._tabbrowserTabs._dndPanel = document.createXULElement("panel");
+              this._tabbrowserTabs._dndPanel.className = "dragfeedback-tab";
+              this._tabbrowserTabs._dndPanel.setAttribute("type", "drag");
+              let wrapper = document.createElementNS(
+                "http://www.w3.org/1999/xhtml",
+                "div"
+              );
+              wrapper.style.width = "160px";
+              wrapper.style.height = "90px";
+              wrapper.appendChild(canvas);
+              this._tabbrowserTabs._dndPanel.appendChild(wrapper);
+              document.documentElement.appendChild(
+                this._tabbrowserTabs._dndPanel
+              );
+            }
+            toDrag = this._tabbrowserTabs._dndPanel;
           }
-          toDrag = this._tabbrowserTabs._dndPanel;
+          // PageThumb is async with e10s but that's fine
+          // since we can update the image during the dnd.
+          PageThumbs.captureToCanvas(browser, canvas)
+            .then(captureListener)
+            .catch(e => console.error(e));
+        } else {
+          // For the non e10s case we can just use PageThumbs
+          // sync, so let's use the canvas for setDragImage.
+          PageThumbs.captureToCanvas(browser, canvas).catch(e =>
+            console.error(e)
+          );
+          dragImageOffset = dragImageOffset * scale;
         }
-        // PageThumb is async with e10s but that's fine
-        // since we can update the image during the dnd.
-        PageThumbs.captureToCanvas(browser, canvas)
-          .then(captureListener)
-          .catch(e => console.error(e));
-      } else {
-        // For the non e10s case we can just use PageThumbs
-        // sync, so let's use the canvas for setDragImage.
-        PageThumbs.captureToCanvas(browser, canvas).catch(e =>
-          console.error(e)
-        );
-        dragImageOffset = dragImageOffset * scale;
+        dt.setDragImage(toDrag, dragImageOffset, dragImageOffset);
       }
-      dt.setDragImage(toDrag, dragImageOffset, dragImageOffset);
 
       // _dragData.offsetX/Y give the coordinates that the mouse should be
       // positioned relative to the corner of the new window created upon
diff --git a/browser/components/tabbrowser/content/tabbrowser.js b/browser/components/tabbrowser/content/tabbrowser.js
--- a/browser/components/tabbrowser/content/tabbrowser.js
+++ b/browser/components/tabbrowser/content/tabbrowser.js
@@ -10150,7 +10150,8 @@ var TabBarVisibility = {
 
     let hasVerticalTabs =
       !isSingleTabWindow &&
-      Services.prefs.getBoolPref("sidebar.verticalTabs", false);
+      Services.prefs.getBoolPref("sidebar.verticalTabs", false) &&
+      !Services.prefs.getBoolPref("sidebar.verticalTabs.showHorizontal", false);
 
     // When `gBrowser` has not been initialized, we're opening a new window and
     // assume only a single tab is loading.
diff --git a/browser/locales/en-US/browser/preferences/preferences.ftl b/browser/locales/en-US/browser/preferences/preferences.ftl
--- a/browser/locales/en-US/browser/preferences/preferences.ftl
+++ b/browser/locales/en-US/browser/preferences/preferences.ftl
@@ -2773,6 +2773,8 @@ hilal-workspaces-pinned-public =
     .label = Make pinned tabs visible in all workspaces
 hilal-workspaces-groups-public =
     .label = Make tab groups visible in all workspaces
+hilal-urlbar-show-workspace-name =
+    .label = Show workspace name in URL bar
 hilal-workspaces-reset-description = Reset all workspace data (cannot be undone).
 hilal-workspaces-reset-button =
     .label = Reset Workspaces
@@ -2787,6 +2789,8 @@ hilal-sidebar-compact-enabled =
 hilal-sidebar-compact-hide-toolbar =
     .label = Auto-hide top toolbar in compact mode
 hilal-sidebar-compact-hide-toolbar-desc = When enabled, the top toolbar containing the address bar and tabs is automatically hidden in compact mode. Hovering near the top edge reveals it with a gorgeous premium glassmorphic slide-down animation. When disabled, the top toolbar remains permanently visible.
+hilal-sidebar-show-horizontal-tabs =
+    .label = Keep top horizontal tabs visible when using vertical tabs
 hilal-sidebar-footer-items-label = Standard Footer Items:
 hilal-sidebar-footer-bookmarks =
     .label = Bookmarks
@@ -2840,6 +2844,9 @@ hilal-privacy-extreme =
 ## UI Enhancements
 
 hilal-uifix-header = UI Enhancements
+hilal-tahoe-enabled =
+    .label = Enable Tahoe Safari shell
+hilal-tahoe-enabled-desc = Uses a macOS Tahoe-inspired Liquid Glass toolbar, sidebar, URL pill, and rounded content frame.
 hilal-uifix-enabled =
     .label = Enable UI Enhancements (includes Firefox-UI-Fix by @black7375)
 hilal-uifix-connect-to-window =
@@ -2891,4 +2898,3 @@ hilal-shortcuts-header = Keyboard Shortcuts
 hilal-shortcuts-description = Customize keyboard shortcuts for Hilal actions and native browser functions.
 hilal-shortcuts-reset =
     .label = Restore Defaults
-
